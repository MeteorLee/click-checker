# ADR-002 PostgreSQL 선택 이유

## 배경
Click-Checker는 이벤트 수집 후 기간·경로·조직 단위로 집계하는 서비스이다.  
즉, 단순 CRUD가 아닌 **집계 중심 조회 서비스**다.

## 선택 이유

- **강력한 집계 기능**
   - date_trunc 기반 시간 단위 집계
   - GROUP BY 및 윈도우 함수 지원
- 복합 인덱스 전략에 적합
- 멀티테넌트 구조에 적합

## 결론
집계 중심 서비스 구조에 적합한 PostgreSQL을 선택하였다.
