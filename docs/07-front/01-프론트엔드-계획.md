# 프론트엔드 계획 (v1.4)

## 목표
- Click-Checker에 브라우저에서 직접 사용할 수 있는 관리자 콘솔 화면을 추가한다.
- 프런트는 `회원가입/로그인 -> organization 선택/생성 -> overview -> 상세 분석 -> 설정/운영` 흐름을 닫는다.
- 기존 백엔드의 JWT 인증, admin analytics API, organization / API key / 규칙 / 멤버 관리 API를 그대로 활용한다.

---

## 1. 기본 방향

- 프런트는 현재 저장소 안에서 함께 관리한다.
- 코드 위치는 루트 아래 `frontend/` 디렉토리로 둔다.
- 프런트 문서는 `docs/07-front` 아래에 둔다.

---

## 2. 기술 방향

- 프레임워크: Next.js
- UI 시스템: Mantine
- 상태 관리: React 기본 상태와 단순 API 호출 중심
- 인증: `Authorization: Bearer <accessToken>`
- 로컬 개발 포트:
  - frontend `3001`
  - backend `8080`

### API 사용 범위
- `signup`
- `login`
- `me`
- organization 생성
- organization membership 해제(삭제 UI)
- admin `overview`
- admin `routes`
- admin `event-types`
- admin `trends`
- admin `users`
- admin `activity`
- admin `retention`
- admin `funnel`
- route template CRUD
- event type mapping CRUD
- members CRUD
- API key metadata / rotate

---

## 3. 화면 범위

### 포함(in scope)
- 회원가입 화면
- 로그인 화면
- organization 선택 화면
- organization 생성 흐름
- organization 삭제(=membership 해제) 흐름
- overview 대시보드 화면
- routes 상세 화면
- event types 상세 화면
- trends 상세 화면
- users 화면
- activity 화면
- retention 화면
- funnels 화면
- route 규칙 관리 화면
- event type 규칙 관리 화면
- 멤버 관리 화면
- API key 1회 표시 / metadata / rotate 흐름

### 제외(out of scope)
- 이메일/토큰 기반 초대 수락
- organization 상태 모델
- 복잡한 차트 시각화
- 모바일 우선 최적화
- 다크 모드

---

## 4. 사용자 흐름

1. 사용자는 회원가입 또는 로그인 화면에서 `loginId`, `password`를 입력한다.
2. 성공 시 access token을 저장한다.
3. 프런트는 `/api/v1/admin/me`를 호출해 사용자가 속한 organization membership 목록을 가져온다.
4. 사용자는 organization 선택 화면에서 기존 organization을 고르거나 새 organization을 만든다.
5. organization 생성 시 API key를 1회 확인하고 복사할 수 있다.
6. 프런트는 선택된 `organizationId`로 overview를 호출한다.
7. overview에서 상세 분석 화면과 설정/운영 화면으로 이동한다.

```text
/signup or /login
-> /organizations
-> organization 선택 또는 생성
-> 생성 시 API key 1회 확인
-> /dashboard/[organizationId]
-> 상세 분석 / 설정 화면 이동
```

---

## 5. 화면별 역할

### 5.1 회원가입 / 로그인
- 계정 생성 또는 로그인
- access token 확보

### 5.2 organization 선택
- 접근 가능한 organization 목록 표시
- organization 생성
- organization 삭제 UI
  - 실제 동작은 membership 해제

### 5.3 overview
- overview 조회
- API key metadata 조회 / rotate
- analytics 허브 역할
- 설정/운영 허브 역할

### 5.4 analytics 상세
- routes
- event types
- trends
- users
- activity
- retention
- funnels

### 5.5 설정 / 운영
- route 규칙 관리
- event type 규칙 관리
- 멤버 관리

---

## 6. 디자인 방향

- 운영 콘솔형 분석 대시보드
- 밝은 배경, 카드 중심 구조
- 숫자와 상태가 잘 읽히는 UI
- overview는 허브, 상세 분석과 설정은 명확히 분리

---

## 7. API 계약 요약

### 인증/조직
- `POST /api/v1/admin/auth/signup`
- `POST /api/v1/admin/auth/login`
- `GET /api/v1/admin/me`
- `POST /api/v1/admin/organizations`
- `DELETE /api/v1/admin/organizations/{organizationId}/members/membership`

### analytics
- `GET /api/v1/admin/organizations/{organizationId}/analytics/overview`
- `GET /api/v1/admin/organizations/{organizationId}/analytics/routes`
- `GET /api/v1/admin/organizations/{organizationId}/analytics/event-types`
- `GET /api/v1/admin/organizations/{organizationId}/analytics/trends`
- `GET /api/v1/admin/organizations/{organizationId}/analytics/users`
- `GET /api/v1/admin/organizations/{organizationId}/analytics/activity`
- `GET /api/v1/admin/organizations/{organizationId}/analytics/retention`
- `POST /api/v1/admin/organizations/{organizationId}/analytics/funnels/report`

### 운영/설정
- `GET /api/v1/admin/organizations/{organizationId}/api-key`
- `POST /api/v1/admin/organizations/{organizationId}/api-key/rotate`
- `GET/POST/PUT/DELETE /api/v1/admin/organizations/{organizationId}/route-templates`
- `GET/POST/PUT/DELETE /api/v1/admin/organizations/{organizationId}/event-type-mappings`
- `GET /api/v1/admin/organizations/{organizationId}/members`
- `POST /api/v1/admin/organizations/{organizationId}/members/by-login-id`
- `PUT /api/v1/admin/organizations/{organizationId}/members/{memberId}/role`
- `DELETE /api/v1/admin/organizations/{organizationId}/members/{memberId}`

### 공통 원칙
- timezone 파라미터는 보내지 않는다.
- analytics 날짜 해석은 백엔드 기준 `Asia/Seoul` 고정이다.
- analytics 조회 기간은 최대 90일이다.
- retention은 `정확히 N일 뒤`가 아니라 `N일 내 재방문` 기준이다.
- trends는
  - 일 단위: 기간 전용
  - 시간 단위: 하루 전용
- organization 삭제 UI는 membership 해제로 동작한다.
- 혼자 남은 OWNER의 membership 해제는 확인 문구 입력이 필요하다.
