# 04. 대규모 부하 테스트 기록

## 문서 목적

`03-대규모-부하-테스트-런북.md` 기준으로 수행한 부하 테스트 결과를 회차별로 누적 기록한다.  
이번 문서의 목적은 실행 결과를 단순 나열하는 것이 아니라, baseline과 개선 후 재테스트를 같은 형식으로 비교할 수 있게 증거를 축적하는 데 있다.

## 기록 원칙

- 회차마다 같은 항목을 같은 순서로 기록한다.
- 결과는 반드시 `수치 + 그래프 + 짧은 해석` 형태로 남긴다.
- 실행 시점의 커밋 해시 또는 이미지 태그를 남긴다.
- 테스트 데이터 식별 규칙(path prefix, eventType, runId)을 남긴다.
- baseline인지, 개선 후 재검증인지 먼저 명시한다.
- Grafana, Loki, Sentry에서 확인한 특이사항은 별도 메모로 남긴다.

## 현재 기준 baseline

- `W1`
  - 기준 baseline run: `w1-20260319-144500-r50-retest`
  - 커밋 / 이미지: `25111e4` / local docker build
  - 환경: `local`
  - 기준 이유: `apiKeyLastUsedAt` update 완화와 org 재조회 제거 후 다시 확보한 `W1` 50 RPS baseline이며, 이후 동일 코드 상태의 higher-rate run 비교 기준으로 사용한다.
- `R1`
  - 기준 baseline run: `r1-20260319-185654-r10-after-index`
  - 커밋 / 이미지: `810ffaa` / local docker build
  - 환경: `local`
  - 기준 이유: 초기 `R1 r10` dryrun은 `threshold_fail`였고, `events (organization_id, occurred_at)` 인덱스 적용 뒤 같은 dataset과 query window에서 처음 안정 구간으로 확보한 read baseline이다.
- `R2`
  - 기준 baseline run: `r2-20260319-213814-r30`
  - 커밋 / 이미지: `현재 작업 트리` / local docker build
  - 환경: `local`
  - 기준 이유: 같은 datasetVersion과 고정 `7일` query window에서 `30 RPS`까지 안정 구간으로 확인된 첫 route 집계 read baseline이며, 이후 `r50` 한계 구간 비교 기준으로 사용한다.
- `R3`
  - 기준 baseline run: `r3-20260319-221428-r10-dryrun-fix`
  - 커밋 / 이미지: `현재 작업 트리` / local docker build
  - 환경: `local`
  - 기준 이유: 첫 `bucket` 계약 mismatch 수정 뒤 같은 datasetVersion, 같은 `7일` query window, 같은 `HOUR/UTC` 조건에서 처음 안정 구간으로 확보한 time-buckets read baseline이다.
- `M1`
  - 기준 baseline run: `m1-20260320-072140-w10-r10-dryrun`
  - 커밋 / 이미지: `현재 작업 트리` / local docker build
  - 환경: `local`
  - 기준 이유: mixed 전용 dataset snapshot 기준으로 처음 성공한 `10/10` dryrun이며, write/read shared resource 경쟁을 설명하는 첫 유효 baseline이다.

원칙:

- baseline이 바뀌면 이 섹션부터 먼저 갱신한다.
- 이후 run은 가능하면 `compareTo` 기준으로 이 baseline과 연결한다.

## 공통 기록 항목

- 실행 일시
- 대상 환경
- 대상 커밋 해시 / 이미지 태그
- 회차 성격
  - baseline
  - 개선 후 재검증
  - mixed 검증
- 시나리오
  - `W1`
  - `R1`
  - `R2`
  - `R3`
  - `M1`
- 부하 조건
  - warm-up
  - main run
  - cooldown
  - RPS 또는 단계 상승 조건
- 테스트 데이터 식별 규칙
- `k6` 결과
  - `http_req_failed`
  - `p95 / p99`
  - `RPS`
- 산출물 경로
- Grafana 확인 항목
  - latency
  - error rate
  - DB active connections
  - Hikari pool usage
- 로그 / Sentry 특이사항
- 1차 해석
- 다음 액션
- 실행 신뢰도

## Round 1~N 요약

- 초기 baseline과 주요 개선 run이 3개 이상 쌓이면 이 섹션을 먼저 갱신한다.
- 오래된 Round의 상세는 유지하되, 상단에는 요약만 배치한다.
- `Round 1`: 첫 공식 `r50` baseline. `50 RPS`는 통과했지만 `dropped_iterations=24`, `vus max=114`가 보여 higher-rate 확인 필요 신호가 있었다.
- `Round 2`: 첫 `r100` baseline. `p95 6.39s`, `dropped_iterations=468`, `vus max=500`으로 local 기준 한계 구간이 드러났다.
- `Round 3`: `apiKeyLastUsedAt` update 완화와 org 재조회 제거 후 `r50` 재검증. `p95 111ms`, `dropped_iterations=0`, `vus max=29`로 같은 `50 RPS`를 훨씬 가볍게 처리했다.
- `Round 4`: 같은 변경 뒤 `r100` 재검증. 이전 failure 구간이 `p95 471ms`, `dropped_iterations=21`, `vus max=208` 수준의 통과 구간으로 이동했다.
- `Round 5`: `R1` 첫 `r10` dryrun. `p95 14.98s`, `dropped_iterations=88`로 overview read가 예상보다 훨씬 무겁다는 신호가 나왔다.
- `Round 6`: `events (organization_id, occurred_at)` 인덱스 적용 후 `R1 r10` 재검증. `p95 130ms`, `dropped_iterations=0`으로 첫 read baseline이 안정 구간으로 이동했다.
- `Round 7`: 같은 인덱스 상태에서 `R1 r30` 실행. `p95 6.52s`, `dropped_iterations=66`으로 여전히 한계 구간이었다.
- `Round 8`: overview 내부 raw path/raw event type 집계 재사용 후 `R1 r30` 재검증. `p95 5.09s`, `dropped_iterations=40`으로 일부 개선됐지만 아직 threshold는 넘지 못했다.
- `Round 9`: `R2 r10` 초기 baseline. 같은 read dataset을 재사용하는 `routes` 집계 read는 `p95 37.55ms`로 매우 가볍게 시작했고, 집계형 read가 요약형 read보다 훨씬 가볍다는 점이 드러났다.
- `Round 10`: `R2 r30` baseline. `p95 76.23ms`, `dropped_iterations=0`으로 여전히 안정 구간이었고, route group-by / topN은 local 기준 아직 충분한 여유가 있었다.
- `Round 11`: `R2 r50` 한계 구간 확인. `p95 5.98s`, `dropped_iterations=119`, `vus max=181`로 threshold fail이 발생해, `routes` read의 local 기준 새 한계 구간이 `30과 50 사이`로 좁혀졌다.
- `Round 12`: `R3 r10` 유효 baseline. 첫 `bucket` 계약 mismatch 수정 뒤 `p95 67.17ms`, `dropped_iterations=0`으로 time-buckets read의 첫 안정 구간이 확보됐다.
- `Round 13`: `R3 r30` 한계 구간 확인. `p95 4.14s`, `dropped_iterations=147`, `vus max=178`로 threshold fail이 발생해, time-bucket read의 local 기준 한계 구간이 `10과 30 사이`로 좁혀졌다.
- `Round 14`: `M1 w10/r10` mixed baseline. 같은 snapshot에서 write/read를 50:50으로 동시에 붙였을 때 read `p95 941ms`, write `p95 468ms`, `dropped_iterations=0`으로 첫 mixed 안정 구간이 확보됐다.
- `Round 15`: `M1 w20/r20` 한계 구간 확인. read `p95 3.78s`, write `p95 3.79s`, `dropped_iterations=77`, `vus max=153`으로 mixed 한계 구간이 `10/10과 20/20 사이`로 좁혀졌다.
- `Round 16`: `W1 r100` prod-direct 검증. EC2 내부 direct port 기준 `p95 22.19ms`, `p99 138.67ms`, 실패율 `0%`로 local에서 확보한 write 안정 구간이 실제 인프라에서도 그대로 재현됐다.
- `Round 17`: `R1 r10` prod-direct 검증. EC2 / RDS 조건에서도 overview read `p95 96.39ms`, `p99 138.28ms`, 실패율 `0%`로 local 안정 구간이 그대로 유지됐다.
- `Round 18`: `M1 w10/r10` prod-direct 검증. read `p95 111.53ms`, write `p95 44.78ms`, 실패율 `0%`로 mixed 첫 안정 구간도 실제 인프라에서 재현됐다.
- `Round 19`: `R1 r50` prod-direct 확장 검증. `p95 9.49s`, `p99 11s`, `dropped_iterations=154`로 prod-direct overview read 한계 구간이 `30과 50 사이`로 좁혀졌다.
- `Round 20`: `M1 w30/r30` prod-direct 확장 검증. read `p95 209.34ms`, write `p95 55.61ms`, 실패율 `0%`로 mixed는 prod-direct에서 아직 `30/30`도 안정 구간으로 확인됐다.
- `Round 21`: `M1 w40/r40` prod-direct 확장 검증. read `p95 6.33s`, write `p95 6.05s`, `dropped_iterations=182`로 prod-direct mixed 한계 구간이 `30/30과 40/40 사이`로 좁혀졌다.
- `Round 22`: `W1 r100` prod-public 최종 확인. `p95 25.84ms`, `p99 92.2ms`, 실패율 `0%`로 public 경로에서도 write stable baseline이 그대로 유지됐다.
- `Round 23`: `R1 r30` prod-public 최종 확인. `p95 388.46ms`, `p99 620.17ms`, 실패율 `0%`로 public 경로에서도 overview read 안정 구간이 유지됐다.
- `Round 24`: `M1 w30/r30` prod-public 최종 확인. read `p95 194.25ms`, write `p95 61.75ms`, 실패율 `0%`로 public 경로에서도 mixed stable baseline이 유지됐다.
- `Round 25`: `W2 s1` 첫 local v2 baseline. 기존 `100 RPS` 시작선은 `p95 16.91s`, 실패율 `2.08%`, achieved throughput `~63 RPS`로 baseline이 아니라 limit 구간에 가까웠다.
- `Round 26`: `R4 v1 s1` 첫 local v2 baseline. 집계 read mix baseline은 `p95 53.93ms`, 실패율 `0%`로 매우 안정적이었다.
- `Round 27`: `R5 v1 s1` 첫 local v2 baseline. 분석 read mix baseline은 `p95 270.67ms`, 실패율 `0%`로 안정 구간이 확인됐다.
- `Round 28`: `M2 s1` 첫 local v2 baseline. read `p95 1.81s`, write `p95 1.41s`, 실패율 `0%`로 threshold는 통과했지만 dropped `165`가 있어 약간 빡빡한 상태였다.
- `Round 29`: `W2 60 RPS` 재보정 probe. `p95 187.82ms`, 실패율 `0%`, dropped `0`로 새 write baseline 후보가 확인됐다.
- `Round 30`: `W2 80 RPS` 재보정 probe. `p95 83.2ms`, 실패율 `0%`, dropped `0`로 stretch 구간도 안정적으로 통과했다.
- `Round 31`: `R6 s1` 첫 local combined read baseline. `p95 183.12ms`, 실패율 `0%`로 `R4 + R5`를 합친 상태에서도 안정 구간이 확인됐다.
- `Round 32`: `M2 s2` local pool 확장 재검증. `maximum-pool-size=40` 적용 뒤에도 throughput `120.47 req/s`, read `p95 12.55s`, write `p95 11.74s`, dropped `22540`로 거의 개선되지 않아 pool 기본값은 주원인이 아니라는 점이 드러났다.
- `Round 33`: `M2 s2` local `EventUser` upsert 재검증. throughput `136.11 req/s`, read `p95 9.80s`, write `p95 9.21s`, dropped `17327`로 mixed는 여전히 fail이지만 write 경로 최적화 효과가 실제로 확인됐다.
- `Round 34`: `W2 s3` local `EventUser` upsert 재검증. 기존 실패 구간이던 `100 RPS`가 `p95 176.1ms`, 실패율 `0%`, achieved `~97 RPS`로 성공 구간으로 이동했다.
- `Round 35`: `W2 120` 상단 probe. `p95 1.41s`, 실패율 `0%`, achieved `~116 RPS`, dropped `105`로 `100` 위쪽도 통과했지만 상단 구간 신호가 보이기 시작했다.
- `Round 36`: `W2 140` 상단 probe. `p95 2.12s`, 실패율 `0%`, achieved `~136 RPS`, dropped `59`로 `140`까지도 통과해 write 상한이 최소 `140+`까지 올라간 것으로 확인됐다.
- `Round 37`: `W2 160` 상단 probe. `p95 1.42s`, 실패율 `0%`, achieved `~155 RPS`, dropped `22`로 `160`도 통과해 write-only 상한이 계속 열려 있음을 확인했다.
- `Round 38`: `W2 200` 상단 probe. `p95 989ms`, 실패율 `0%`, achieved `~191 RPS`, dropped `660`으로 `200`도 통과했지만 limit band 신호가 더 분명해졌다.
- `Round 39`: `W2 250` 상단 probe. `p95 7.61s`, `p99 8.81s`, achieved `~213 RPS`, dropped `4606`으로 threshold fail이 발생해 상한이 `250` 아래라는 점이 드러났다.
- `Round 40`: `W2 300` 상단 probe. `p95 11.02s`, `p99 13.26s`, achieved `~199 RPS`, dropped `15789`로 명확한 failure band에 들어갔다.
- `Round 41`: `M2 s2` Spring Cache + Caffeine 첫 적용. 앱 재기동 직후 cold cache run은 throughput `121.64 req/s`, read `p95 17.10s`, write `p95 14.29s`, dropped `21999`로 오히려 악화되어 cold-start penalty가 크게 드러났다.
- `Round 42`: `M2 s2` Spring Cache + Caffeine rerun. steady-state rerun은 throughput `164.40 req/s`, read `p95 7.51s`, write `p95 7.19s`, dropped `8376`으로 정상화됐지만 threshold는 여전히 넘지 못했다.
- `Round 43`: `R4 v2 s2` time-series DB bucket 적용 재검증. `p95 57.2ms`, `p99 136.82ms`, 실패율 `0%`로 trend/aggregate read가 local stage 2에서도 매우 안정적으로 동작함을 확인했다.
- `Round 44`: `M2 s2` time-series DB bucket 적용 재검증. throughput `169.76 req/s`, read `p95 8.13s`, write `p95 7.51s`, dropped `6843`로 mixed는 여전히 fail이지만 read query 본체 비용 절감이 실제로 먹혔다.
- `Round 45`: `M2 s2` overview summary 단일 쿼리 첫 적용. 앱 재기동 직후 첫 run은 throughput `117.80 req/s`, read `p95 13.48s`, write `p95 12.73s`, dropped `22969`로 크게 악화되어 첫 결과만으로는 채택 근거가 부족했다.
- `Round 46`: `M2 s2` overview summary 단일 쿼리 steady-state rerun. rerun은 throughput `173.11 req/s`, read `p95 8.08s`, write `p95 7.30s`, dropped `6009`로 회복됐고, time-series DB bucket 기준 best보다 소폭 개선돼 keep 판단 근거가 생겼다.
- `Round 47`: `M2 s2` unique-user raw pair 경량화 첫 적용. 앱 재기동 직후 첫 run은 throughput `133.61 req/s`, read `p95 13.91s`, write `p95 12.57s`, dropped `18260`으로 다시 cold-start outlier 성격의 악화를 보였다.
- `Round 48`: `M2 s2` unique-user raw pair 경량화 steady-state rerun. rerun은 throughput `188.76 req/s`, read `p95 4.63s`, write `p95 4.30s`, dropped `977`로 크게 회복됐고, 처음으로 `M2 s2` threshold를 모두 통과했다.
- `Round 49`: `M2 s2` aggregate regrouping raw count 정렬 제거 첫 적용. throughput `187.35 req/s`, read `p95 6.02s`, write `p95 5.64s`, dropped `2047`로 수치는 나쁘지 않았지만 threshold는 넘지 못했고, unique-user raw pair 기준 best를 재현하지도 못했다.
- `Round 50`: `M2 s2` aggregate regrouping raw count 정렬 제거 steady-state rerun. rerun은 throughput `164.27 req/s`, read `p95 9.12s`, write `p95 8.30s`, dropped `8784`로 오히려 더 나빠졌고, `EXPLAIN ANALYZE`상 ordered/unordered 차이도 수 ms 수준에 그쳐 perf win으로 채택하긴 어렵다고 판단했다.
- `Round 51`: `M2 s2` overview 응답 `5분` 캐시 + `2m` warm-up 재검증. app restart 직후 first-run 편차를 줄이기 위해 warm-up을 늘린 결과 throughput `193.94 req/s`, read `p95 1.24s`, write `p95 1.11s`, dropped `29`로 매우 안정적으로 threshold를 통과했다. 이번 결과는 overview cache keep 근거이면서도, local `M2` 판단에서 warm-up 프로토콜이 매우 중요하다는 점을 함께 보여준다.

## Round 25~31 - v2 local baseline 1차

v2 realistic load test의 첫 local baseline은 [38-v2-local-baseline-1차-종합.md](38-v2-local-baseline-1차-종합.md)를 기준으로 본다.

| Round | 시나리오 | runId | 결과 | 핵심 수치 | 1차 해석 |
| --- | --- | --- | --- | --- | --- |
| 25 | `W2 s1` | `w2-20260322-100328-s1-baseline` | `threshold_fail` | `p95 16.91s`, fail `2.08%`, achieved `~63 RPS` | 기존 `100 RPS` 시작선은 realistic write baseline이 아니라 limit probe에 가까웠다 |
| 26 | `R4 v1 s1` | `r4-20260322-101011-s1-v1-baseline` | `success` | `p95 53.93ms`, fail `0%` | 집계 read mix baseline은 충분히 안정적이다 |
| 27 | `R5 v1 s1` | `r5-20260322-102232-s1-v1-baseline` | `success` | `p95 270.67ms`, fail `0%` | 분석 read mix baseline도 안정 구간이다 |
| 28 | `M2 s1` | `m2-20260322-103144-s1-baseline` | `success` | read `p95 1.81s`, write `p95 1.41s`, dropped `165` | threshold는 통과했지만 realistic mixed는 이미 약간 빡빡하다 |
| 29 | `W2 60 probe` | `w2-20260322-105941-r60-probe` | `success` | `p95 187.82ms`, fail `0%`, dropped `0` | 새 baseline 후보 `60 RPS`가 안정적으로 통과했다 |
| 30 | `W2 80 probe` | `w2-20260322-114415-r80-probe` | `success` | `p95 83.2ms`, fail `0%`, dropped `0` | `80 RPS`도 stable이며 stage 2 stretch 후보가 된다 |
| 31 | `R6 s1` | `r6-20260322-120522-s1-baseline` | `success` | `p95 183.12ms`, fail `0%` | combined read baseline도 안정적으로 확보됐다 |

## Round 32~34 - v2 local 후속 개선 검증

| Round | 시나리오 | runId | 결과 | 핵심 수치 | 1차 해석 |
| --- | --- | --- | --- | --- | --- |
| 32 | `M2 s2` | `m2-20260322-133431-s2-after-pool` | `threshold_fail` | throughput `120.47 req/s`, read `p95 12.55s`, write `p95 11.74s`, dropped `22540` | local pool headroom 확대만으로는 realistic mixed 포화를 해소하지 못했다 |
| 33 | `M2 s2` | `m2-20260322-135955-s2-after-user-upsert` | `threshold_fail` | throughput `136.11 req/s`, read `p95 9.80s`, write `p95 9.21s`, dropped `17327` | `EventUser` select-then-insert 제거는 mixed에도 유의미하게 먹혔지만 아직 threshold 통과엔 부족하다 |
| 34 | `W2 s3` | `w2-20260322-142157-s3-after-user-upsert` | `success` | `p95 176.1ms`, `p99 789.83ms`, fail `0%`, achieved `~97 RPS` | 같은 변경은 write-only에서는 매우 크게 먹혀 기존 `100 RPS` 실패 구간을 성공 구간으로 이동시켰다 |
| 35 | `W2 120 probe` | `w2-20260322-144348-r120-probe-after-upsert` | `success` | `p95 1.41s`, `p99 3.42s`, fail `0%`, achieved `~116 RPS`, dropped `105` | `100` 위 상단 구간도 통과했고 write 상한이 다시 열렸지만, dropped가 생겨 upper band 신호가 보이기 시작했다 |
| 36 | `W2 140 probe` | `w2-20260322-144801-r140-probe-after-upsert` | `success` | `p95 2.12s`, `p99 3.2s`, fail `0%`, achieved `~136 RPS`, dropped `59` | `140`까지도 threshold 내에서 통과해 write-only 기준 새 상한이 최소 `140+`로 올라갔다 |
| 37 | `W2 160 probe` | `w2-20260322-150107-r160-probe-after-upsert` | `success` | `p95 1.42s`, `p99 2.46s`, fail `0%`, achieved `~155 RPS`, dropped `22` | `160`도 통과해 write-only 상단 구간이 더 위로 이동했고, 아직 실패 band는 아니었다 |
| 38 | `W2 200 probe` | `w2-20260322-151049-r200-probe-after-upsert` | `success` | `p95 989.51ms`, `p99 4.35s`, fail `0%`, achieved `~191 RPS`, dropped `660` | `200`도 threshold 내에서 통과했지만 dropped가 늘어 limit band에 가까워졌다 |
| 39 | `W2 250 probe` | `w2-20260322-154024-r250-probe-after-upsert` | `threshold_fail` | `p95 7.61s`, `p99 8.81s`, fail `0%`, achieved `~213 RPS`, dropped `4606` | `250`에서 처음 명확한 threshold fail이 발생해 write-only 상한이 `200과 250 사이`로 좁혀졌다 |
| 40 | `W2 300 probe` | `w2-20260322-155023-r300-probe-after-upsert` | `threshold_fail` | `p95 11.02s`, `p99 13.26s`, fail `0%`, achieved `~199 RPS`, dropped `15789` | `300`은 명확한 failure band로, 개선 후에도 write-only ceiling은 `250` 아래에 있음을 재확인했다 |

## Round 1 - Baseline

### 실행 정보

- 실행 일시: `2026-03-19 13:22:21 ~ 13:35:49 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `d7bad34` / local docker build
- 회차 성격: baseline
- 시나리오: `W1`

### 이전 Round 대비 변경점

- 첫 공식 baseline run
- 드라이런 대비 `warm-up 2m + main 10m + cooldown 1m` 정식 조건 적용
- `capture.mode=scripted` 기준 renderer PNG 자동 캡처 적용

### 부하 조건

- warm-up: `2m`
- main run: `10m`
- cooldown: `1m`
- RPS / 단계 상승: `50 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=10`, `perf-w1-r50-20260319132007`
- path prefix: `/loadtest/w1`
- eventType: `loadtest_write`
- runId: `w1-20260319-132007-r50`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `696.45ms`
- `p99`: `1.77s` (`console.log` threshold output 기준)
- 평균 RPS: `48.48 req/s`
- 기타 요약: `dropped_iterations=24`, `iterations=29977`, `vus max=114`, `max latency=4.96s`
- 산출물 디렉터리: `artifacts/perf/w1/w1-20260319-132007-r50/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, `p95/p99` 추세 확인 가능
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `ingest-rps.png`, `ingest-latency.png`, `db-connections.png`, `db-pool.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 실행 산출물 기준 별도 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `50 RPS`에서는 write path가 아직 포화 전 단계로 보이지만, 후반부 VU 급증 구간이 있어 지연 스파이크는 존재한다.
- 근거 지표: 실패율 `0%`, `p95 696ms`, `p99 1.77s`로 threshold 이내였고, `dropped_iterations=24`, `vus max=114`가 발생했다.
- 반례 / 불확실성: local single-tenant 기준이라 prod direct/public, 실제 RDS 지연, multi-tenant 영향은 반영되지 않았다.

### 다음 액션

- P0: 같은 조건으로 `r100` baseline 실행
- P1: `r200` baseline 실행 후 `50/100/200` 전후 비교
- P2: higher rate에서 DB/Hikari PNG와 dropped iterations 상관관계 확인

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 정식 조건 run이 끝까지 성공했고, `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 생성됐다.

---

## Round 2

### 실행 정보

- 실행 일시: `2026-03-19 13:56:03 ~ 14:00:32 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `d7bad34` / local docker build
- 회차 성격: 개선 후 재검증 아님, baseline 확장 run
- 시나리오: `W1`

### 이전 Round 대비 변경점

- `r50`에서 `r100`으로 rate만 상향
- `compareTo=w1-20260319-132007-r50`
- fresh org / fresh API key / 기존 사용자 1000명 seed는 동일 구조 유지

### 부하 조건

- warm-up: `2m`
- main run: `10m` 설정이었지만 threshold fail로 조기 종료
- cooldown: `1m` 설정이었지만 threshold fail 구간에서 run 종료
- RPS / 단계 상승: `100 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=11`, `perf-w1-r100-20260319135509`
- path prefix: `/loadtest/w1`
- eventType: `loadtest_write`
- runId: `w1-20260319-135509-r100`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `6.39s`
- `p99`: `8.12s` (`console.log` threshold output 기준)
- 평균 RPS: `90.60 req/s`
- 기타 요약: `dropped_iterations=468`, `iterations=10086`, `301 interrupted`, `vus max=500`, `Insufficient VUs` 경고 발생
- 산출물 디렉터리: `artifacts/perf/w1/w1-20260319-135509-r100/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, `r50` 대비 급격한 지연 상승 해석 가능
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `ingest-rps.png`, `ingest-latency.png`, `db-connections.png`, `db-pool.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: run 산출물 기준 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `100 RPS`에서는 write path가 local 기준 한계 구간에 들어섰고, DB/Hikari 또는 write 경로 대기 비용이 급격히 증가한 것으로 보인다.
- 근거 지표: `p95 6.39s`, `p99 8.12s`, `dropped_iterations=468`, `vus max=500`, `Insufficient VUs` 경고, 평균 처리량도 목표 `100`에 못 미친 `90.60 req/s`
- 반례 / 불확실성: local single-tenant 환경이라 prod direct RDS 지연, 네트워크 RTT, multi-tenant 간섭은 아직 반영되지 않았다.

### 다음 액션

- P0: `r100`은 local 기준 임계점 신호로 판단하고 `r200`은 보수적으로 진행 여부를 다시 판단
- P1: `r50`과 `r100` PNG를 함께 보고 DB/Hikari 상승 양상 비교
- P2: 이후 write 경로 해부 시 event user lookup/create, flush/save, connection 대기 지점을 우선 확인

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: threshold fail run이지만 `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 남았고, 병목 신호도 충분히 명확하다.

---

## Round 3 - `r50` 재검증

### 실행 정보

- 실행 일시: `2026-03-19 14:45:31 ~ 14:58:59 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `25111e4` / local docker build
- 회차 성격: 개선 후 재검증
- 시나리오: `W1`

### 이전 Round 대비 변경점

- `apiKeyLastUsedAt`를 매 요청마다 저장하지 않고 최근 1분 이내면 저장을 건너뛰도록 조정
- 서비스 내부의 org 재조회를 제거하고 JPA reference 사용으로 대체
- `compareTo=w1-20260319-132007-r50`

### 부하 조건

- warm-up: `2m`
- main run: `10m`
- cooldown: `1m`
- RPS / 단계 상승: `50 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=12`, `perf-w1-r50-20260319144500`
- path prefix: `/loadtest/w1`
- eventType: `loadtest_write`
- runId: `w1-20260319-144500-r50-retest`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `111.12ms`
- `p99`: `344.7ms` (`console.log` threshold output 기준)
- 평균 RPS: `48.56 req/s`
- 기타 요약: `dropped_iterations=0`, `iterations=30001`, `vus max=29`, `max latency=1.92s`
- 산출물 디렉터리: `artifacts/perf/w1/w1-20260319-144500-r50-retest/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, 기존 `r50` 대비 지연 구간이 크게 낮아졌다.
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `ingest-rps.png`, `ingest-latency.png`, `db-connections.png`, `db-pool.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 코드 변경 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 초기 `r50`에서 보이던 핫패스 부가 비용 중, `apiKeyLastUsedAt` 과도한 update와 org 재조회가 실제 성능 부담이었던 것으로 보인다.
- 근거 지표: 같은 `50 RPS`에서 `p95 696ms -> 111ms`, `dropped_iterations 24 -> 0`, `vus max 114 -> 29`로 개선됐다.
- 반례 / 불확실성: 여전히 local single-tenant 기준이며, user lookup/create 비용은 그대로 남아 있다.

### 다음 액션

- P0: 같은 변경 상태로 `r100` 재검증
- P1: `r50` 개선 효과가 higher-rate 구간에도 유지되는지 확인
- P2: 필요 시 다음 병목 후보(user lookup/create)를 다시 분석

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 정식 조건 run이 끝까지 성공했고 산출물 3종과 renderer PNG가 모두 남았다.

---

## Round 4 - `r100` 재검증

### 실행 정보

- 실행 일시: `2026-03-19 15:04:11 ~ 15:17:38 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `25111e4` / local docker build
- 회차 성격: 개선 후 재검증
- 시나리오: `W1`

### 이전 Round 대비 변경점

- `apiKeyLastUsedAt` update 완화 + org 재조회 제거 상태 유지
- `compareTo=w1-20260319-135509-r100`
- fresh org / fresh API key / 기존 사용자 1000명 seed는 동일 구조 유지

### 부하 조건

- warm-up: `2m`
- main run: `10m`
- cooldown: `1m`
- RPS / 단계 상승: `100 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=13`, `perf-w1-r100-20260319150314`
- path prefix: `/loadtest/w1`
- eventType: `loadtest_write`
- runId: `w1-20260319-150314-r100-retest`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `471.20ms`
- `p99`: `1.44s` (`console.log` threshold output 기준)
- 평균 RPS: `96.97 req/s`
- 기타 요약: `dropped_iterations=21`, `iterations=59980`, `vus max=208`, `max latency=4.91s`
- 산출물 디렉터리: `artifacts/perf/w1/w1-20260319-150314-r100-retest/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, 기존 `r100` 대비 지연이 큰 폭으로 낮아졌다.
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `ingest-rps.png`, `ingest-latency.png`, `db-connections.png`, `db-pool.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 코드 변경 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 이전 `r100` failure 구간을 만들던 앞단 부가 비용이 실제 병목의 핵심 축이었다.
- 근거 지표: `p95 6.39s -> 471ms`, `dropped_iterations 468 -> 21`, `vus max 500 -> 208`로 개선됐고 threshold도 모두 통과했다.
- 반례 / 불확실성: user lookup/create 비용과 local single-tenant 한계는 그대로라서, 더 높은 rate나 prod direct에서는 다른 병목이 다시 나타날 수 있다.

### 다음 액션

- P0: 이번 변경 내용을 문서에 반영하고 `r50/r100` 전후 비교를 확정
- P1: `r200`를 짧게 볼지, 바로 user lookup/create 분석으로 갈지 판단
- P2: prod direct로 옮길 때 실행 위치 / 요청 대상 / Grafana 캡처 소스를 분리 기록

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 정식 조건 run이 끝까지 성공했고 `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 남았다.

---

## 누적 비교표

| Round | 시나리오 | 기준 | 변경 | 환경 | 커밋/이미지 | `http_req_failed` | `p95` | `p99` | 핵심 해석 | 다음 액션 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Round 1 | `W1` | baseline | 첫 정식 `r50`, scripted capture 적용 | `local` | `d7bad34` / local build | `0.00%` | `696.45ms` | `1.77s` | `50 RPS`는 통과했지만 VU 급증과 dropped iterations가 보여 higher rate 확인 필요 | `r100` baseline 실행 |
| Round 2 | `W1` | compareTo `r50` | `r100` 상향, threshold fail | `local` | `d7bad34` / local build | `0.00%` | `6.39s` | `8.12s` | `100 RPS`는 local 기준 한계 구간이며 VU 상한과 dropped iterations가 명확하다 | `r200` 진행 여부 재판단 또는 write 경로 해부 |
| Round 3 | `W1` | compareTo `r50` | `apiKeyLastUsedAt` update 완화 + org 재조회 제거 후 `r50` 재검증 | `local` | `25111e4` / local build | `0.00%` | `111.12ms` | `344.7ms` | 같은 `50 RPS`를 훨씬 적은 부담으로 처리했고 앞단 부가 비용이 실제 병목 후보였음이 드러났다 | `r100` 재검증 |
| Round 4 | `W1` | compareTo 기존 `r100` | 같은 변경 후 `r100` 재검증 | `local` | `25111e4` / local build | `0.00%` | `471.20ms` | `1.44s` | 이전 failure 구간이 통과 구간으로 이동했고 `100 RPS`는 더 이상 local 한계 구간으로 보기 어렵다 | 문서 반영 후 `r200` 또는 다음 병목 분석 판단 |
| Round 5 | `R1` | dryrun | 첫 overview read baseline 시도 | `local` | `810ffaa` / local build | `0.00%` | `14.98s` | `17.6s` | `10 RPS`에서도 overview read가 실패했고, read path가 예상보다 훨씬 무겁다는 신호가 드러났다 | overview 쿼리 구조와 인덱스 후보 분석 |
| Round 6 | `R1` | compareTo 초기 `r10` | `events (organization_id, occurred_at)` 인덱스 추가 후 `r10` 재검증 | `local` | `810ffaa` / local build | `0.00%` | `130.84ms` | `167.14ms` | 첫 read 인덱스 하나로 `10 RPS`가 안정 구간으로 이동했고 기본 범위 읽기 비용이 핵심 원인이었음이 드러났다 | `r30`로 상향해 새 한계 구간 확인 |
| Round 7 | `R1` | compareTo `r10` 안정 baseline | 같은 인덱스 상태에서 `r30` 실행 | `local` | `810ffaa` / local build | `0.00%` | `6.52s` | `8.45s` | 첫 인덱스로도 `30 RPS`는 여전히 한계 구간이며, 남은 비용은 overview 내부 조합 비용 쪽으로 좁혀졌다 | 중복 집계 구조 검토 |
| Round 8 | `R1` | compareTo 기존 `r30` | overview 내부 raw path/raw event type 집계 재사용 후 `r30` 재검증 | `local` | `810ffaa` / local build | `0.00%` | `5.09s` | `6.79s` | `30 RPS`가 일부 개선됐지만 아직 threshold fail이며, 두 번째 인덱스보다 구조 재사용 개선이 먼저 먹혔다 | `R1` 1차 종합 후 `R2` 또는 추가 read 전략 판단 |
| Round 9 | `R2` | 초기 baseline | `routes` 집계 read 첫 dryrun | `local` | 현재 작업 트리 / local build | `0.00%` | `37.55ms` | `55.97ms` | `R2 routes`는 `R1 overview`보다 훨씬 가볍고, 첫 `10 RPS`는 충분히 안정 구간이었다 | `r30`으로 상향해 집계형 read 여유 구간 확인 |
| Round 10 | `R2` | compareTo `r10` | 같은 datasetVersion, 같은 `7일` 구간에서 `r30` 실행 | `local` | 현재 작업 트리 / local build | `0.00%` | `76.23ms` | `124.91ms` | route group-by / topN read는 `30 RPS`까지도 안정 구간이었고, `overview`보다 훨씬 높은 처리 여유가 확인됐다 | `r50`로 상향해 첫 한계 구간 확인 |
| Round 11 | `R2` | compareTo 기존 `r30` | 같은 조건에서 `r50` 실행 | `local` | 현재 작업 트리 / local build | `0.00%` | `5.98s` | `8.65s` | `routes` read도 `50 RPS`에서는 threshold fail이 발생했고, local 기준 한계 구간이 `30과 50 사이`로 좁혀졌다 | `R2` 1차 종합 후 `R3` 설계 |
| Round 12 | `R3` | 초기 baseline | `bucket` 계약 mismatch 수정 후 `time-buckets` 첫 유효 dryrun | `local` | 현재 작업 트리 / local build | `0.00%` | `67.17ms` | `103.2ms` | `R3 time-buckets`는 첫 유효 `10 RPS`에서 충분히 안정 구간이었고, 추세형 read가 `overview`보다 훨씬 가볍다는 점이 확인됐다 | `r30`으로 상향해 첫 한계 구간 확인 |
| Round 13 | `R3` | compareTo `r10` | 같은 datasetVersion, 같은 `7일/HOUR/UTC` 조건에서 `r30` 실행 | `local` | 현재 작업 트리 / local build | `0.00%` | `4.14s` | `7.99s` | `time-buckets` read는 `30 RPS`에서 threshold fail이 발생했고, local 기준 한계 구간이 `10과 30 사이`로 좁혀졌다 | `R3` 1차 종합 후 `M1` 설계 |
| Round 14 | `M1` | 초기 baseline | mixed dataset snapshot 기준 `w10/r10` 첫 유효 dryrun | `local` | 현재 작업 트리 / local build | `0.00%` | read `941ms` / write `468ms` | read `1.10s` / write `697ms` | mixed 전용 snapshot에서 write/read를 같이 붙여도 `10/10`은 안정 구간이었고, 첫 mixed baseline으로 사용 가능했다 | `w20/r20`으로 상향해 shared resource 경쟁 시작 구간 확인 |
| Round 15 | `M1` | compareTo `w10/r10` | 같은 snapshot 기준 `w20/r20` mixed run 실행 | `local` | 현재 작업 트리 / local build | `0.00%` | read `3.78s` / write `3.79s` | read `5.87s` / write `6.61s` | mixed에서는 read만이 아니라 write도 함께 느려졌고, local 기준 한계 구간이 `10/10과 20/20 사이`로 좁혀졌다 | `M1` 1차 종합 후 전체 write/read/mixed 지도 정리 |
| Round 16 | `W1` | prod-direct 최소 검증 | direct port 기준 `r100` write 재검증 | `prod-direct` | 현재 prod 배포본 / EC2 direct | `0.00%` | `22.19ms` | `138.67ms` | local에서 확보한 write 안정 구간이 EC2 direct에서도 그대로 재현됐고, 첫 prod-direct write 검증은 성공으로 정리됐다 | `R1 r10` prod-direct 검증 |
| Round 17 | `R1` | prod-direct 최소 검증 | direct port와 RDS 기준 `r10` overview read 검증 | `prod-direct` | 현재 prod 배포본 / EC2 direct | `0.00%` | `96.39ms` | `138.28ms` | overview read 안정 구간도 EC2 / RDS에서 재현됐고, local에서 본 첫 read baseline 방향이 실제 인프라와 크게 어긋나지 않았다 | `M1 10/10` prod-direct 검증 |
| Round 18 | `M1` | prod-direct 최소 검증 | direct port와 RDS 기준 `w10/r10` mixed 검증 | `prod-direct` | 현재 prod 배포본 / EC2 direct | `0.00%` | read `111.53ms` / write `44.78ms` | read `190.20ms` / write `68.94ms` | mixed 첫 안정 구간도 실제 인프라에서 재현됐고, prod-direct 최소 검증 세트가 모두 성공으로 닫혔다 | prod-direct 결과 종합 후 다음 단계 판단 |
| Round 19 | `R1` | prod-direct 확장 검증 | direct port와 RDS 기준 `r50` overview read 검증 | `prod-direct` | 현재 prod 배포본 / EC2 direct | `0.00%` | `9.49s` | `11s` | local보다 여유는 컸지만 prod-direct에서도 `overview`는 결국 `50 RPS`에서 한계 구간으로 들어갔고, 실제 한계 위치는 `30과 50 사이`로 정리됐다 | `M1 w30/r30` prod-direct 검증 |
| Round 20 | `M1` | prod-direct 확장 검증 | direct port와 RDS 기준 `w30/r30` mixed 검증 | `prod-direct` | 현재 prod 배포본 / EC2 direct | `0.00%` | read `209.34ms` / write `55.61ms` | read `272.24ms` / write `84.05ms` | local에서 한계였던 mixed도 prod-direct에서는 `30/30`까지 안정 구간이었고, mixed 실제 여유가 local보다 훨씬 크다는 점이 드러났다 | prod-direct 확장 결과 종합 후 public 검증 또는 추가 mixed 확장 판단 |
| Round 21 | `M1` | prod-direct 확장 검증 | direct port와 RDS 기준 `w40/r40` mixed 검증 | `prod-direct` | 현재 prod 배포본 / EC2 direct | `0.00%` | read `6.33s` / write `6.05s` | read `8.32s` / write `7.23s` | prod-direct mixed도 `40/40`에서는 read와 write가 함께 무너졌고, 실제 한계 위치가 `30/30과 40/40 사이`로 좁혀졌다 | prod-direct 결과 종합 후 public 검증 또는 구조 개선 우선순위 판단 |
| Round 22 | `W1` | prod-public 최종 확인 | public 도메인 기준 `r100` write 검증 | `prod-public` | 현재 prod 배포본 / nginx + TLS + public route | `0.00%` | `25.84ms` | `92.2ms` | public 경로에서도 write stable baseline이 그대로 유지됐고, direct 대비 추가 오버헤드는 작았다 | `R1 r30` prod-public 검증 |
| Round 23 | `R1` | prod-public 최종 확인 | public 도메인 기준 `r30` overview read 검증 | `prod-public` | 현재 prod 배포본 / nginx + TLS + public route | `0.00%` | `388.46ms` | `620.17ms` | public 경로에서 overview read 지연은 direct보다 올라갔지만 안정 구간은 유지됐고, 공개 진입점 오버헤드가 baseline을 깨지는 않았다 | `M1 w30/r30` prod-public 검증 |
| Round 24 | `M1` | prod-public 최종 확인 | public 도메인 기준 `w30/r30` mixed 검증 | `prod-public` | 현재 prod 배포본 / nginx + TLS + public route | `0.00%` | read `194.25ms` / write `61.75ms` | read `283.41ms` / write `92.01ms` | public 경로에서도 mixed stable baseline이 유지됐고, direct 대비 소폭 상승은 있었지만 경쟁 구간으로 밀릴 정도는 아니었다 | prod-public 결과 종합 후 전체 성능 개선 1차 마감 판단 |

## 문서 사용 원칙

- 실제 실험을 수행할 때는 기존 템플릿을 복사해 `Round 3`, `Round 4`처럼 누적한다.
- 같은 시나리오의 baseline과 개선 후 결과가 생기면 누적 비교표를 먼저 갱신한다.
- 실행 중 혼선이나 비정상 문제가 생기면 [05-성능-개선-트러블슈팅.md](05-성능-개선-트러블슈팅.md)에 분리 기록한다.
- 한 사이클(`테스트 -> 개선 -> 재테스트`)이 끝나면 [07-성능-개선-종합.md](07-성능-개선-종합.md)에서 최종 정리한다.

---

## Round 5 - `R1 r10` 초기 dryrun

### 실행 정보

- 실행 일시: `2026-03-19 18:09:40 ~ 18:10:22 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `810ffaa` / local docker build
- 회차 성격: dryrun
- 시나리오: `R1`

### 이전 Round 대비 변경점

- 첫 공식 read 시나리오 `R1` 실행
- read 전용 dataset org와 고정 query window를 처음 사용
- `overview` read가 실제로 어느 정도 무거운지 확인하는 최초 run

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `10 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-18T09:00:00Z ~ 2026-03-19T09:00:00Z`
- runId: `r1-20260319-180937-r10-dryrun`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `14.98s`
- `p99`: `17.6s`
- 평균 RPS: `5.10 req/s`
- 기타 요약: `dropped_iterations=88`, `iterations=212`, `vus max=60`, `max latency=19.43s`
- 산출물 디렉터리: `artifacts/perf/r1/r1-20260319-180937-r10-dryrun/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, analytics latency가 즉시 급등하는 흐름 확인 가능
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 기능 오류는 없었고 read 응답도 정상 200을 유지했다
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `overview` read는 단일 count API가 아니라 여러 count/group by/distinct를 한 요청에 묶어 수행하고 있어, 기본 범위 읽기 비용 자체가 예상보다 큰 것으로 보인다.
- 근거 지표: `10 RPS`에서도 `p95 14.98s`, `dropped_iterations=88`, 평균 처리량 `5.10 req/s`로 목표 rate를 따라가지 못했다.
- 반례 / 불확실성: dataset이 처음 준비된 상태라 read path의 어느 계산이 가장 무거운지는 아직 분해되지 않았다.

### 다음 액션

- P0: overview 쿼리 구조와 `events` 조회 인덱스 후보 확인
- P1: 첫 공통 인덱스 적용 후 같은 `r10` 조건으로 재검증
- P2: `countDistinct`와 path/event type 집계 중 무엇이 더 비싼지 추가 확인

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: dryrun이지만 산출물과 PNG가 모두 남았고 실패 신호가 충분히 선명했다.

---

## Round 6 - `R1 r10` 인덱스 후 재검증

### 실행 정보

- 실행 일시: `2026-03-19 18:56:54 ~ 18:57:39 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `810ffaa` / local docker build
- 회차 성격: 개선 후 재검증 및 read baseline 재확보
- 시나리오: `R1`

### 이전 Round 대비 변경점

- `events (organization_id, occurred_at)` 인덱스 적용
- `compareTo=r1-20260319-180937-r10-dryrun`
- 같은 dataset, 같은 query window 유지

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `10 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-18T09:00:00Z ~ 2026-03-19T09:00:00Z`
- runId: `r1-20260319-185654-r10-after-index`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `130.84ms`
- `p99`: `167.14ms`
- 평균 RPS: `9.70 req/s`
- 기타 요약: `dropped_iterations=0`, `iterations=436`, `vus max=20`, `max latency=417.8ms`
- 산출물 디렉터리: `artifacts/perf/r1/r1-20260319-185654-r10-after-index/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, 초기 `r10` 대비 analytics latency가 급격히 안정화됨
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 인덱스 적용 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 초기 `R1`의 가장 큰 원인은 overview 조립 자체보다도 `events` 범위 읽기 비용에 더 가까웠다.
- 근거 지표: 같은 `10 RPS`에서 `p95 14.98s -> 130.84ms`, `dropped_iterations 88 -> 0`, `vus max 60 -> 20`으로 크게 개선됐다.
- 반례 / 불확실성: 높은 rate에서 path/event type 집계 중복과 countDistinct 비용이 다시 도드라질 수 있다.

### 다음 액션

- P0: 같은 인덱스 상태로 `r30` 실행
- P1: write 회귀가 없는지 `W1 r100` 짧은 확인
- P2: `30 RPS`에서도 실패하면 overview 중복 구조 재사용 쪽을 우선 검토

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 동일 dataset으로 재검증했고 개선 폭이 명확하며 산출물도 모두 남았다.

---

## Round 7 - `R1 r30` 인덱스 후 한계 구간 확인

### 실행 정보

- 실행 일시: `2026-03-19 19:00:13 ~ 19:02:10 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `810ffaa` / local docker build
- 회차 성격: 개선 후 higher-rate 확인
- 시나리오: `R1`

### 이전 Round 대비 변경점

- 동일 인덱스 상태 유지
- `r10`에서 `r30`으로 rate만 상향
- `compareTo=r1-20260319-185654-r10-after-index`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `30 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-18T09:00:00Z ~ 2026-03-19T09:00:00Z`
- runId: `r1-20260319-190013-r30-after-index`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `6.52s`
- `p99`: `8.45s`
- 평균 RPS: `23.33 req/s`
- 기타 요약: `dropped_iterations=66`, `iterations=770`, `vus max=126`, `max latency=10.33s`
- 산출물 디렉터리: `artifacts/perf/r1/r1-20260319-190013-r30-after-index/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, `30 RPS`에서 analytics latency가 다시 threshold를 넘는 흐름 확인 가능
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: read 응답은 정상 200을 유지
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 첫 인덱스로 기본 범위 읽기 비용은 많이 줄었지만, overview 내부의 여러 집계 조합 비용은 여전히 높다.
- 근거 지표: `30 RPS`에서 `p95 6.52s`, `dropped_iterations=66`, 평균 처리량 `23.33 req/s`로 목표 rate를 따라가지 못했다.
- 반례 / 불확실성: `countDistinct`가 주원인인지, path/event type 집계 중복이 더 큰지 아직 완전히 분리되진 않았다.

### 다음 액션

- P0: `countDistinct`와 path/event type 집계의 실제 비용을 추가 확인
- P1: overview 내부 raw path/raw event type 집계 중복 제거 설계
- P2: 두 번째 인덱스 추가는 write 회귀 가능성을 고려해 보류

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 dataset, 같은 query window, 같은 인덱스 상태에서 rate만 상향한 비교라 해석이 명확하다.

---

## Round 8 - `R1 r30` overview 집계 재사용 후 재검증

### 실행 정보

- 실행 일시: `2026-03-19 19:30:43 ~ 19:32:02 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `810ffaa` / local docker build
- 회차 성격: 개선 후 재검증
- 시나리오: `R1`

### 이전 Round 대비 변경점

- overview 내부 raw path/raw event type 집계를 각 1회만 수행하도록 재사용 구조 적용
- `compareTo=r1-20260319-190013-r30-after-index`
- 같은 dataset, 같은 query window 유지

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `30 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-18T09:00:00Z ~ 2026-03-19T09:00:00Z`
- runId: `r1-20260319-193043-r30-after-overview-dedupe`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `5.09s`
- `p99`: `6.79s`
- 평균 RPS: `26.03 req/s`
- 기타 요약: `dropped_iterations=40`, `iterations=861`, `vus max=96`, `max latency=8.09s`
- 산출물 디렉터리: `artifacts/perf/r1/r1-20260319-193043-r30-after-overview-dedupe/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, 기존 `r30` 대비 analytics latency가 일부 낮아졌다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: overview 리팩터링 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: path/event type 집계 중복은 실제 비용이었지만, 이를 줄여도 `30 RPS` 전체를 안정 구간으로 옮기기엔 아직 부족하다.
- 근거 지표: `p95 6.52s -> 5.09s`, `dropped_iterations 66 -> 40`, `vus max 126 -> 96`으로 개선됐지만 여전히 `threshold_fail`이다.
- 반례 / 불확실성: 두 번째 인덱스 없이도 추가 개선 여지가 있을 수 있으나, 현재 수치만으로는 인덱스 추가가 필수라고 단정하긴 어렵다.

### 다음 액션

- P0: 현재 결과를 `R1` 1차 종합으로 정리
- P1: 추가 인덱스보다는 `R2`로 넘어가기 전에 현재 read 병목 해석을 문서화
- P2: 필요 시 이후 `R1` 2차 단계에서 두 번째 인덱스 또는 더 큰 구조 변경을 재검토

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 dataset, 같은 query window, 같은 rate에서 코드 변경 효과만 비교했고 개선 폭도 일관되게 관찰됐다.

---

## Round 9 - `R2 r10` 초기 baseline

### 실행 정보

- 실행 일시: `2026-03-19 21:35:16 ~ 21:36:19 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: dryrun
- 시나리오: `R2`

### 이전 Round 대비 변경점

- `R2 routes` 집계 read 첫 실행
- `R1` datasetVersion `r1-v1` 재사용
- 고정 `7일` query window와 `top=10` query preset 첫 적용

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `10 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-12T09:00:00Z ~ 2026-03-19T09:00:00Z`
- top: `10`
- runId: `r2-20260319-213513-r10-dryrun`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `37.55ms`
- `p99`: `55.97ms` (`console.log` threshold output 기준)
- 평균 RPS: `9.56 req/s`
- 기타 요약: `dropped_iterations=0`, `iterations=300`, `vus max=20`
- 산출물 디렉터리: `artifacts/perf/r2/r2-20260319-213513-r10-dryrun/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, analytics latency가 매우 낮은 범위에서 유지됐다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 첫 `R2` 단계에선 집계형 read 자체가 병목으로 보이지 않았다.
- 근거 지표: `p95 37.55ms`, `p99 55.97ms`, `dropped_iterations=0`, `vus max=20`
- 반례 / 불확실성: `10 RPS`는 충분히 낮은 구간이라 실제 한계는 더 높은 rate에서 확인해야 한다.

### 다음 액션

- P0: 같은 조건으로 `r30` 실행
- P1: `R1 overview`와 `R2 routes`의 비용 차이를 종합 문서에서 같이 설명
- P2: 필요 시 `paths`, `routes/unmatched-paths` 보조 확인 추가

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 datasetVersion과 query preset을 유지했고, `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 생성됐다.

---

## Round 10 - `R2 r30` 안정 구간 확인

### 실행 정보

- 실행 일시: `2026-03-19 21:40:36 ~ 21:41:29 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: baseline 확장 run
- 시나리오: `R2`

### 이전 Round 대비 변경점

- `compareTo=r2-20260319-213513-r10-dryrun`
- datasetVersion, query window, `top`은 동일 유지
- rate만 `30 RPS`로 상향

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `30 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-12T09:00:00Z ~ 2026-03-19T09:00:00Z`
- top: `10`
- runId: `r2-20260319-213814-r30`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `76.23ms`
- `p99`: `124.91ms` (`console.log` threshold output 기준)
- 평균 RPS: `29.12 req/s`
- 기타 요약: `dropped_iterations=0`, `iterations=900`, `vus max=60`
- 산출물 디렉터리: `artifacts/perf/r2/r2-20260319-213814-r30/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, `30 RPS`에서도 analytics latency가 낮게 유지됐다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: route group-by / topN read는 local 기준 `30 RPS`에서도 아직 병목 구간으로 보기 어렵다.
- 근거 지표: `p95 76.23ms`, `p99 124.91ms`, `dropped_iterations=0`, `vus max=60`
- 반례 / 불확실성: `50 RPS`로 올리면 집계 비용과 정렬 비용이 급격히 늘 수 있다.

### 다음 액션

- P0: 같은 조건으로 `r50` 실행
- P1: `R2`는 `R1`보다 훨씬 가볍다는 점을 종합 문서에서 분명히 남김
- P2: `r50` 결과에 따라 `R2` 1차 종료 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 datasetVersion, 같은 query window, 같은 `top`을 유지했고 renderer PNG까지 모두 정상 생성됐다.

---

## Round 11 - `R2 r50` 한계 구간 확인

### 실행 정보

- 실행 일시: `2026-03-19 21:42:32 ~ 21:43:27 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: 한계 구간 확인 run
- 시나리오: `R2`

### 이전 Round 대비 변경점

- `compareTo=r2-20260319-213814-r30`
- datasetVersion, query window, `top`은 동일 유지
- rate만 `50 RPS`로 상향

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `50 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-12T09:00:00Z ~ 2026-03-19T09:00:00Z`
- top: `10`
- runId: `r2-20260319-214222-r50`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `5.98s`
- `p99`: `8.65s` (`console.log` threshold output 기준)
- 평균 RPS: `40.30 req/s`
- 기타 요약: `dropped_iterations=119`, `iterations=1382`, `vus max=181`
- 산출물 디렉터리: `artifacts/perf/r2/r2-20260319-214222-r50/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, `r30` 대비 analytics latency 급상승 확인
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `db-connections.png`, `db-pool.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: threshold fail 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `routes` 집계 read도 `50 RPS`에서는 정렬 / group-by / route 재집계 비용이 겹치며 local 기준 한계 구간에 진입한다.
- 근거 지표: `p95 5.98s`, `p99 8.65s`, `dropped_iterations=119`, `vus max=181`, 평균 처리량도 목표 `50`에 못 미친 `40.30 req/s`
- 반례 / 불확실성: `R2`는 `R1 overview`보다 훨씬 가볍지만, 그것이 곧 prod direct에서도 같은 비율로 유지된다는 뜻은 아니다.

### 다음 액션

- P0: `R2` 1차 종합 문서 작성
- P1: `R3 time-buckets` 시나리오로 이동
- P2: `M1` 이전에 read 종류별 비용 차이를 한 번 더 정리

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 datasetVersion과 query preset에서 rate만 바꿔 실행했고, threshold fail run도 `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 남았다.

---

## Round 12 - `R3 r10` 유효 baseline

### 실행 정보

- 실행 일시: `2026-03-19 22:14:28 ~ 22:15:09 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: dryrun
- 시나리오: `R3`

### 이전 Round 대비 변경점

- `R3 time-buckets` 첫 유효 baseline run
- `bucket` enum 계약을 `HOUR`로 맞춘 뒤 첫 정상 실행
- `R1` datasetVersion `r1-v1`와 고정 `7일` query window 재사용

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `10 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-12T09:00:00Z ~ 2026-03-19T09:00:00Z`
- bucket: `HOUR`
- timezone: `UTC`
- runId: `r3-20260319-221428-r10-dryrun-fix`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `67.17ms`
- `p99`: `103.2ms` (`console.log` threshold output 기준)
- 평균 RPS: `9.70 req/s`
- 기타 요약: `dropped_iterations=0`, `iterations=300`, `vus max=20`, `max latency=197.04ms`
- 산출물 디렉터리: `artifacts/perf/r3/r3-20260319-221428-r10-dryrun-fix/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, analytics latency가 낮은 범위에서 안정적으로 유지됐다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 기능 오류 없이 정상 200 응답 유지
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 첫 유효 `R3` 단계에선 time-bucket 조립 자체가 병목으로 보이지 않았다.
- 근거 지표: `p95 67.17ms`, `p99 103.2ms`, `dropped_iterations=0`, `vus max=20`
- 반례 / 불확실성: 첫 실패 run은 성능 이슈가 아니라 계약 mismatch였으므로, 현재 baseline은 수정 후 첫 유효 run 기준으로 본다.

### 다음 액션

- P0: 같은 datasetVersion, 같은 `7일/HOUR/UTC` 조건으로 `r30` 실행
- P1: `R2 routes`와 `R3 time-buckets`의 비용 차이를 종합 문서에서 같이 설명
- P2: `r30` 결과에 따라 `R3` 1차 종료 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 계약 mismatch 수정 뒤 동일 dataset과 query preset에서 정상 실행됐고, 산출물과 PNG도 모두 남았다.

---

## Round 13 - `R3 r30` 한계 구간 확인

### 실행 정보

- 실행 일시: `2026-03-19 22:23:31 ~ 22:35:13 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: 한계 구간 확인 run
- 시나리오: `R3`

### 이전 Round 대비 변경점

- `compareTo=r3-20260319-221428-r10-dryrun-fix`
- datasetVersion, query window, `bucket`, `timezone`은 동일 유지
- rate만 `30 RPS`로 상향

### 부하 조건

- warm-up: `2m`
- main run: `10m`
- cooldown: `1m`
- RPS / 단계 상승: `30 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=17`, `perf-r1-dataset`
- dataset version: `r1-v1`
- query window: `2026-03-12T09:00:00Z ~ 2026-03-19T09:00:00Z`
- bucket: `HOUR`
- timezone: `UTC`
- runId: `r3-20260319-222330-r30`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `4.14s`
- `p99`: `7.99s` (`console.log` threshold output 기준)
- 평균 RPS: `28.71 req/s`
- 기타 요약: `dropped_iterations=147`, `iterations=17854`, `vus max=178`, `max latency=19.94s`
- 산출물 디렉터리: `artifacts/perf/r3/r3-20260319-222330-r30/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, main run 후반부에 analytics latency가 크게 상승했다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `analytics-rps.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: threshold fail 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `time-buckets` read는 `R2 routes`보다 무겁고, hour bucket 조립 비용이 동시 부하에서 빠르게 누적되는 것으로 보인다.
- 근거 지표: `p95 4.14s`, `p99 7.99s`, `dropped_iterations=147`, `vus max=178`, 평균 처리량도 목표 `30`에 못 미친 `28.71 req/s`
- 반례 / 불확실성: 같은 총 이벤트 수에서도 bucket 수나 기간이 바뀌면 비용 곡선이 달라질 수 있으므로, 현재 해석은 `7일/HOUR/UTC` 고정 조건에 한정된다.

### 다음 액션

- P0: `R3` 1차 종합 문서 작성
- P1: `M1` mixed baseline 설계로 이동
- P2: 이후 필요 시 `DAY` bucket이나 확장 trend API를 별도 회차로 분리 검토

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 datasetVersion과 query preset에서 rate만 상향했고, threshold fail run도 `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 남았다.

---

## Round 14 - `M1 w10/r10` mixed baseline

### 실행 정보

- 실행 일시: `2026-03-20 07:21:40 ~ 07:22:57 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: mixed baseline
- 시나리오: `M1`

### 이전 Round 대비 변경점

- 첫 mixed 시나리오 `M1` 실행
- mixed 전용 datasetVersion `m1-v1`, snapshotVersion `m1-v1-snap1` 기준 첫 유효 run
- write `10 RPS` + read `10 RPS`를 같은 snapshot에서 동시에 실행

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: write/read `10/10` pair rate

### 테스트 데이터 식별 규칙

- dataset version: `m1-v1`
- snapshot version: `m1-v1-snap1`
- write path prefix: `/loadtest/m1`
- write eventType: `loadtest_mixed`
- read endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- read query window: `2026-03-18T22:00:00Z ~ 2026-03-19T22:00:00Z`
- runId: `m1-20260320-072140-w10-r10-dryrun`

### k6 결과

- `http_req_failed`: `0.00%`
- read `p95`: `941.11ms`
- read `p99`: `1.10s` (`console.log` threshold output 기준)
- write `p95`: `467.54ms`
- write `p99`: `697.02ms` (`console.log` threshold output 기준)
- 기타 요약: `dropped_iterations=0`, `iterations=602`, `vus max=40`, `read max latency=1.71s`, `write max latency=1.32s`
- 산출물 디렉터리: `artifacts/perf/m1/m1-20260320-072140-w10-r10-dryrun/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, read/write latency 모두 안정 범위에서 유지됐다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `api-request-mix.png`, `ingest-latency.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: 유효 run 기준 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `10/10` mixed 구간에서는 shared resource 경쟁이 아직 명확한 병목으로 보이지 않았다.
- 근거 지표: read `p95 941ms`, write `p95 467ms`, `dropped_iterations=0`, `vus max=40`
- 반례 / 불확실성: 첫 mixed 유효 run이며 main run이 `30s`라서, 장시간 main run 기준 한계 구간 해석은 다음 회차에서 확인이 필요하다.

### 다음 액션

- P0: 같은 snapshot 기준 `w20/r20` mixed run 실행
- P1: read와 write가 동시에 느려지는지 확인
- P2: `20/20` 결과에 따라 mixed 한계 구간과 다음 단계 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: snapshot restore와 mixed run, renderer PNG가 모두 정상 동작했고 첫 mixed baseline 산출물이 온전히 남았다.

---

## Round 15 - `M1 w20/r20` 한계 구간 확인

### 실행 정보

- 실행 일시: `2026-03-20 07:27:39 ~ 07:32:39 (+09:00)`
- 대상 환경: `local`
- 대상 커밋 해시 / 이미지 태그: `현재 작업 트리` / local docker build
- 회차 성격: 한계 구간 확인 run
- 시나리오: `M1`

### 이전 Round 대비 변경점

- 같은 datasetVersion, 같은 snapshotVersion, 같은 read query window 유지
- `compareTo=m1-20260320-072140-w10-r10-dryrun`
- rate만 write/read `20/20`으로 상향

### 부하 조건

- warm-up: `30s`
- main run: `3m`
- cooldown: `0`
- RPS / 단계 상승: write/read `20/20` pair rate

### 테스트 데이터 식별 규칙

- dataset version: `m1-v1`
- snapshot version: `m1-v1-snap1`
- write path prefix: `/loadtest/m1`
- write eventType: `loadtest_mixed`
- read endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- read query window: `2026-03-18T22:00:00Z ~ 2026-03-19T22:00:00Z`
- runId: `m1-20260320-072739-w20-r20`

### k6 결과

- `http_req_failed`: `0.00%`
- read `p95`: `3.78s`
- read `p99`: `5.87s` (`console.log` threshold output 기준)
- write `p95`: `3.79s`
- write `p99`: `6.61s` (`console.log` threshold output 기준)
- 기타 요약: `dropped_iterations=77`, `iterations=7125`, `vus max=153`, `read max latency=10.37s`, `write max latency=11.18s`
- 산출물 디렉터리: `artifacts/perf/m1/m1-20260320-072739-w20-r20/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: renderer PNG 생성 완료, read/write latency 모두 main run 후반부에 크게 상승했다
- error rate: `0%`로 기록됨
- DB active connections: renderer PNG 생성 완료
- Hikari pool usage: renderer PNG 생성 완료
- 캡처 시간 범위: `start-2m ~ end+2m`
- 추가 캡처 메모: `dashboard-overview.png`, `api-request-mix.png`, `ingest-latency.png`, `analytics-latency.png`, `http-error-ratio.png`, `db-connections.png`, `db-pool.png`, `jvm-heap-by-app.png` 자동 생성

### 로그 / Sentry 특이사항

- app log: threshold fail 외 별도 애플리케이션 오류 없음
- nginx log: local baseline이라 해당 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: mixed에서는 `overview` read만이 아니라 write도 함께 느려졌고, shared DB / connection 경쟁이 한계 구간을 더 빨리 만든 것으로 보인다.
- 근거 지표: read `p95 3.78s`, write `p95 3.79s`, `dropped_iterations=77`, `vus max=153`
- 반례 / 불확실성: read query window는 fixed historical range라 결과 값 자체 변화보다 shared resource 경쟁이 더 강조된 조건이다.

### 다음 액션

- P0: `M1` 1차 종합 문서 작성
- P1: `30/30`은 당장 올리지 않고 현재 mixed 한계 구간을 먼저 정리
- P2: 이후 write/read/mixed 전체 결과를 종합해 우선순위 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: 같은 snapshot 기준으로 재현 가능하게 실행됐고, threshold fail run도 `summary.json`, `console.log`, `meta.json`, renderer PNG가 모두 남았다.

---

## Round 16 - `W1 r100` prod-direct 검증

### 실행 정보

- 실행 일시: `2026-03-19 23:36:39 ~ 23:40:12 (UTC)`
- 대상 환경: `prod-direct`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / EC2 direct
- 회차 성격: prod-direct 최소 검증
- 시나리오: `W1`

### 이전 Round 대비 변경점

- local baseline 이후 첫 실제 인프라 검증
- EC2 내부에서 app direct port 대상으로 실행
- Grafana renderer timeout 이슈로 `capture.mode=manual`
- 실행 시점은 `prod-direct` 스크립트 분리 전이라 산출물 경로와 메타 일부가 local 형식으로 남았다

### 부하 조건

- warm-up: `30s`
- main run: `3m`
- cooldown: `0`
- RPS / 단계 상승: `100 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `orgId=74`, `perf-w1-r100-20260319233545`
- path prefix: `/loadtest/w1`
- eventType: `loadtest_write`
- runId: `w1-20260319-233545-r100-prod-direct`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `22.19ms`
- `p99`: `138.67ms`
- 평균 RPS: `99.99 req/s`
- 기타 요약: `iterations=18001`, `vus max=30`, `max latency=1.02s`
- 산출물 디렉터리: `artifacts/perf/w1/w1-20260319-233545-r100-prod-direct/` (`EC2` 실행 시점 기준)
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod Grafana renderer가 일부 패널에서 `60s` timeout이 나와 scripted capture는 보류했다

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: direct 호출 기준이라 public ingress 지표와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 이번 run에서는 write 경로 병목 신호가 보이지 않았다.
- 근거 지표: `p95 22.19ms`, `p99 138.67ms`, 실패율 `0%`, 평균 처리량 `99.99 req/s`
- 반례 / 불확실성: prod-direct 첫 write 확인 run이며, local과 정확히 같은 길이의 main run은 아니므로 절대 수치보다 안정 구간 재현 여부를 먼저 본다.

### 다음 액션

- P0: `R1 r10` prod-direct 검증
- P1: `M1 10/10` prod-direct mixed 검증
- P2: prod-direct 최소 세트 결과를 별도 종합 문서로 정리

### 실행 신뢰도

- 신뢰도:
  - `medium-high`
- 이유: 실행 자체는 성공했고 `summary.json`, `console.log`, `meta.json`이 남았지만, 이 run은 prod-direct 스크립트 분리 전 과도기 경로에 기록됐다.

---

## Round 17 - `R1 r10` prod-direct 검증

### 실행 정보

- 실행 일시: `2026-03-20 01:11:54 ~ 01:12:41 (UTC)`
- 대상 환경: `prod-direct`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / EC2 direct
- 회차 성격: prod-direct 최소 검증
- 시나리오: `R1`

### 이전 Round 대비 변경점

- `prod-direct` 전용 `prepare / run / seed` 구조로 첫 실행
- local postgres container 대신 RDS direct seed 적용
- `env.name=prod-direct`, `dbInstance=prod-rds` 메타 기록 확인
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `10 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `perf-r1-prod-direct-dataset`
- dataset version: `r1-v1`
- query window: `고정 historical 1일`
- endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `r1-20260320-011118-r10-prod-direct`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `96.39ms`
- `p99`: `138.28ms`
- 평균 RPS: `10.01 req/s`
- 기타 요약: `iterations=301`, `vus max=20`, `max latency=294.48ms`
- 산출물 디렉터리: `artifacts/perf/prod-direct/r1/r1-20260320-011118-r10-prod-direct/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 이번 단계에서 보류

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: direct 호출 기준이라 public ingress 지표와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 이번 run에서는 overview read가 병목 구간에 들어서지 않았다.
- 근거 지표: `p95 96.39ms`, `p99 138.28ms`, 실패율 `0%`, 평균 처리량 `10.01 req/s`
- 반례 / 불확실성: prod-direct 최소 검증 세트는 안정 구간만 다시 보는 목적이므로, `30 RPS` 이상 한계 구간 재현 여부는 아직 확인하지 않았다.

### 다음 액션

- P0: `M1 10/10` prod-direct mixed 검증
- P1: local과 prod-direct의 stable baseline 차이를 종합 문서에서 비교
- P2: 필요 시 higher-rate prod-direct 재검증 여부 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: RDS direct seed, `meta.json`, `summary.json`, `console.log`까지 모두 정상 생성됐고 prod-direct 메타도 정확히 기록됐다.

---

## Round 18 - `M1 w10/r10` prod-direct 검증

### 실행 정보

- 실행 일시: `2026-03-20 01:18:18 ~ 01:19:05 (UTC)`
- 대상 환경: `prod-direct`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / EC2 direct
- 회차 성격: prod-direct 최소 검증
- 시나리오: `M1`

### 이전 Round 대비 변경점

- `prod-direct` 전용 mixed dataset / snapshot 구조로 첫 실행
- RDS direct seed와 restore 기준으로 mixed baseline 재현
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: write/read `10/10` pair rate

### 테스트 데이터 식별 규칙

- dataset version: `m1-v1`
- snapshot version: `m1-v1-snap1`
- write path prefix: `/loadtest/m1`
- write eventType: `loadtest_mixed`
- read endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `m1-20260320-011754-w10-r10-prod-direct`

### k6 결과

- `http_req_failed`: `0.00%`
- read `p95`: `111.53ms`
- read `p99`: `190.20ms`
- write `p95`: `44.78ms`
- write `p99`: `68.94ms`
- 기타 요약: `iterations=602`, `vus max=40`, `http_reqs=602`
- 산출물 디렉터리: `artifacts/perf/prod-direct/m1/m1-20260320-011754-w10-r10-prod-direct/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 이번 단계에서 보류

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: direct 호출 기준이라 public ingress 지표와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 이번 run에서는 mixed 첫 안정 구간이 실제 인프라에서도 충분히 유지됐다.
- 근거 지표: read `p95 111.53ms`, write `p95 44.78ms`, 실패율 `0%`
- 반례 / 불확실성: mixed 한계 구간이 실제 인프라에서도 `20/20` 부근인지까지는 아직 확인하지 않았다.

### 다음 액션

- P0: prod-direct 최소 검증 세트 결과를 `27` 종합 문서로 정리
- P1: local과 prod-direct stable baseline 차이를 비교
- P2: 이후 public 검증 또는 higher-rate prod-direct 재검증 우선순위 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: mixed seed, snapshot restore, main run, 메타 기록이 모두 정상 동작했고 실패율도 `0%`였다.

---

## Round 21 - `M1 w40/r40` prod-direct 확장 검증

### 실행 정보

- 실행 일시: `2026-03-20 02:53 ~ 02:54 (UTC, console 기준)`
- 대상 환경: `prod-direct`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / EC2 direct
- 회차 성격: prod-direct 확장 검증
- 시나리오: `M1`

### 이전 Round 대비 변경점

- 같은 datasetVersion, 같은 snapshot 기준으로 write/read를 `40/40`까지 상향
- prod-direct mixed 실제 한계 위치를 확인하기 위한 첫 failure-side 검증
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: write/read `40/40` pair rate

### 테스트 데이터 식별 규칙

- dataset version: `m1-v1`
- snapshot version: `m1-v1-snap1`
- write path prefix: `/loadtest/m1`
- write eventType: `loadtest_mixed`
- read endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `m1-*-w40-r40-prod-direct`

### k6 결과

- `http_req_failed`: `0.00%`
- read `p95`: `6.33s`
- read `p99`: `8.32s`
- write `p95`: `6.05s`
- write `p99`: `7.23s`
- 기타 요약: `dropped_iterations=182`, `iterations=2220`, `vus max=339`, `max latency=10.07s`
- 산출물 디렉터리: `artifacts/perf/prod-direct/m1/<runId>/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 보류

### 로그 / Sentry 특이사항

- app log: threshold fail 외 별도 애플리케이션 오류 없음
- nginx log: direct 호출 기준이라 public ingress 지표와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: prod-direct mixed도 `40/40`에서는 read와 write가 함께 한계 구간에 들어섰다.
- 근거 지표: read `p95 6.33s`, write `p95 6.05s`, `dropped_iterations=182`, `vus max=339`
- 반례 / 불확실성: `50/50` 같은 더 높은 failure 구간은 남아 있지만, mixed 실제 한계 위치를 좁히는 데는 이미 충분하다.

### 다음 액션

- P0: prod-direct mixed 한계 구간을 `30/30과 40/40 사이`로 정리
- P1: `R1`과 `M1` prod-direct limit 결과를 `27` 종합 문서에 반영
- P2: 이후 public 검증 또는 구조 개선 우선순위 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: snapshot restore, main run, 메타 기록이 모두 정상 동작했고 threshold fail run도 수치와 산출물이 충분히 남았다.

---

## Round 22 - `W1 r100` prod-public 최종 확인

### 실행 정보

- 실행 일시: `2026-03-20 (UTC, console 기준)`
- 대상 환경: `prod-public`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / nginx + TLS + public route
- 회차 성격: prod-public 최종 확인
- 시나리오: `W1`

### 이전 Round 대비 변경점

- direct port가 아니라 `https://clickchecker.dev` 기준으로 요청 경로 변경
- dataset seed는 기존처럼 내부에서 수행하되, 실제 k6 요청은 public 도메인 기준으로 전환
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `30s`
- main run: `3m`
- cooldown: `0`
- RPS / 단계 상승: `100 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `perf-w1-r100-*`
- path prefix: `/loadtest/w1`
- eventType: `loadtest_write`
- runId: `w1-*-r100-prod-public`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `25.84ms`
- `p99`: `92.2ms`
- 평균 RPS: `99.99 req/s`
- 기타 요약: `iterations=18001`, `vus max=27`, `max latency=574.7ms`
- 산출물 디렉터리: `artifacts/perf/prod-public/w1/<runId>/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 보류

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: public 경로 기준이라 direct 결과와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 이번 run에서는 public 경로가 write 안정 구간을 깨지 않았다.
- 근거 지표: `p95 25.84ms`, `p99 92.2ms`, 실패율 `0%`, 평균 처리량 `99.99 req/s`
- 반례 / 불확실성: public route와 TLS가 들어갔지만 이번 run은 stable-side 검증이라 public write limit까지는 아직 확인하지 않았다.

### 다음 액션

- P0: public write stable baseline 유지 확인으로 정리
- P1: `R1 r30` prod-public 검증
- P2: 이후 `M1 w30/r30`까지 public 경로 안정 구간 확인

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: public 도메인 기준 main run이 끝까지 성공했고, 수치와 산출물이 모두 정상적으로 남았다.

---

## Round 23 - `R1 r30` prod-public 최종 확인

### 실행 정보

- 실행 일시: `2026-03-20 (UTC, console 기준)`
- 대상 환경: `prod-public`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / nginx + TLS + public route
- 회차 성격: prod-public 최종 확인
- 시나리오: `R1`

### 이전 Round 대비 변경점

- `prod-direct` 안정 구간이었던 `r30`을 public 도메인 기준으로 재검증
- 요청 경로만 public으로 바꾸고, dataset seed는 내부 RDS direct 기준 유지
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `30 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `perf-r1-prod-public-dataset`
- dataset version: `r1-v1`
- query window: `고정 historical 1일`
- endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `r1-*-r30-prod-public`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `388.46ms`
- `p99`: `620.17ms`
- 평균 RPS: `29.98 req/s`
- 기타 요약: `iterations=901`, `vus max=21`, `max latency=746.34ms`
- 산출물 디렉터리: `artifacts/perf/prod-public/r1/<runId>/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 보류

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: public 경로 기준으로 응답 상태 특이사항 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: public 계층은 overview read 지연을 direct보다 올렸지만 안정 구간을 깨지는 않았다.
- 근거 지표: `p95 388.46ms`, `p99 620.17ms`, 실패율 `0%`
- 반례 / 불확실성: public read 한계 구간은 아직 확인하지 않았고, 이번 run은 stable-side 검증에 해당한다.

### 다음 액션

- P0: public overview stable baseline 유지 확인으로 정리
- P1: `M1 w30/r30` prod-public 검증
- P2: 필요 시 이후 public failure-side 구간 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: public 도메인 기준 main run이 끝까지 성공했고, 수치와 산출물이 모두 정상적으로 남았다.

---

## Round 24 - `M1 w30/r30` prod-public 최종 확인

### 실행 정보

- 실행 일시: `2026-03-20 (UTC, console 기준)`
- 대상 환경: `prod-public`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / nginx + TLS + public route
- 회차 성격: prod-public 최종 확인
- 시나리오: `M1`

### 이전 Round 대비 변경점

- `prod-direct` 안정 구간이었던 `w30/r30`을 public 도메인 기준으로 재검증
- public 경로를 통과하는 mixed 기준선 확인
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: write/read `30/30` pair rate

### 테스트 데이터 식별 규칙

- dataset version: `m1-v1`
- snapshot version: `m1-v1-snap1`
- write path prefix: `/loadtest/m1`
- write eventType: `loadtest_mixed`
- read endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `m1-*-w30-r30-prod-public`

### k6 결과

- `http_req_failed`: `0.00%`
- read `p95`: `194.25ms`
- read `p99`: `283.41ms`
- write `p95`: `61.75ms`
- write `p99`: `92.01ms`
- 기타 요약: `iterations=1802`, `vus max=120`, `http_reqs=1802`
- 산출물 디렉터리: `artifacts/perf/prod-public/m1/<runId>/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 보류

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: public 경로 기준으로 응답 상태 특이사항 없음
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: public 계층이 mixed stable baseline을 깨지는 않았다.
- 근거 지표: read `p95 194.25ms`, write `p95 61.75ms`, 실패율 `0%`
- 반례 / 불확실성: public mixed limit은 아직 확인하지 않았고, 이번 run은 stable-side 최종 확인에 해당한다.

### 다음 액션

- P0: public mixed stable baseline 유지 확인으로 정리
- P1: `29` prod-public 종합 문서 작성
- P2: 이후 구조 개선 우선순위 재판단 또는 필요 시 public failure-side 구간 검토

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: public 경로 기준 mixed main run이 끝까지 성공했고, 수치와 산출물이 모두 정상적으로 남았다.

---

## Round 19 - `R1 r50` prod-direct 확장 검증

### 실행 정보

- 실행 일시: `2026-03-20 01:39:27 ~ 01:40:05 (UTC)`
- 대상 환경: `prod-direct`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / EC2 direct
- 회차 성격: prod-direct 확장 검증
- 시나리오: `R1`

### 이전 Round 대비 변경점

- 최소 검증 세트 이후 첫 higher-rate prod-direct read 검증
- 같은 datasetVersion, 같은 query window에서 rate만 `50 RPS`로 상향
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: `50 RPS` 고정 run

### 테스트 데이터 식별 규칙

- organization: `perf-r1-prod-direct-dataset`
- dataset version: `r1-v1`
- query window: `고정 historical 1일`
- endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `r1-*-r50-prod-direct`

### k6 결과

- `http_req_failed`: `0.00%`
- `p95`: `9.49s`
- `p99`: `11s`
- 평균 RPS: `35.55 req/s`
- 기타 요약: `dropped_iterations=154`, `iterations=1347`, `vus max=254`, `max latency=16.19s`
- 산출물 디렉터리: `artifacts/perf/prod-direct/r1/<runId>/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 보류

### 로그 / Sentry 특이사항

- app log: threshold fail 외 별도 애플리케이션 오류 없음
- nginx log: direct 호출 기준이라 public ingress 지표와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: `overview` read는 prod-direct에서도 결국 `50 RPS`에서는 한계 구간에 들어간다.
- 근거 지표: `p95 9.49s`, `p99 11s`, `dropped_iterations=154`, 평균 처리량 `35.55 req/s`
- 반례 / 불확실성: local과 달리 prod-direct에서는 `30 RPS`가 안정 구간이었으므로, 한계 위치는 local보다 높지만 절대 상한은 아직 더 세밀하게 탐색하지 않았다.

### 다음 액션

- P0: `R1` prod-direct 한계 구간을 `30과 50 사이`로 정리
- P1: `M1 w30/r30` prod-direct mixed 검증
- P2: 필요 시 `R1 40` 같은 중간 구간 재탐색 판단

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: prod-direct 메타 기준으로 실행됐고 threshold fail run도 `summary.json`, `console.log`, `meta.json`이 모두 남았다.

---

## Round 20 - `M1 w30/r30` prod-direct 확장 검증

### 실행 정보

- 실행 일시: `2026-03-20 01:44:52 ~ 01:45:29 (UTC)`
- 대상 환경: `prod-direct`
- 대상 커밋 해시 / 이미지 태그: `현재 prod 배포본` / EC2 direct
- 회차 성격: prod-direct 확장 검증
- 시나리오: `M1`

### 이전 Round 대비 변경점

- 최소 검증 세트 이후 첫 higher-rate prod-direct mixed 검증
- 같은 datasetVersion, 같은 snapshot 기준으로 write/read를 `30/30`까지 상향
- Grafana renderer timeout 이슈로 `capture.mode=manual`

### 부하 조건

- warm-up: `15s`
- main run: `30s`
- cooldown: `0`
- RPS / 단계 상승: write/read `30/30` pair rate

### 테스트 데이터 식별 규칙

- dataset version: `m1-v1`
- snapshot version: `m1-v1-snap1`
- write path prefix: `/loadtest/m1`
- write eventType: `loadtest_mixed`
- read endpoint: `GET /api/v1/events/analytics/aggregates/overview`
- runId: `m1-*-w30-r30-prod-direct`

### k6 결과

- `http_req_failed`: `0.00%`
- read `p95`: `209.34ms`
- read `p99`: `272.24ms`
- write `p95`: `55.61ms`
- write `p99`: `84.05ms`
- 기타 요약: `iterations=1802`, `vus max=120`, `http_reqs=1802`
- 산출물 디렉터리: `artifacts/perf/prod-direct/m1/<runId>/`
- `summary.json`: 생성됨
- `console.log`: 생성됨
- `meta.json`: 생성됨

### Grafana 확인

- latency: 자동 PNG 캡처는 생략
- error rate: `0%`로 기록됨
- DB active connections: 수동 대시보드 확인 기준 특이사항 없음
- Hikari pool usage: 수동 대시보드 확인 기준 특이사항 없음
- 캡처 시간 범위: `manual`
- 추가 캡처 메모: prod renderer timeout 이슈로 scripted capture는 보류

### 로그 / Sentry 특이사항

- app log: 오류 없이 정상 200 응답 유지
- nginx log: direct 호출 기준이라 public ingress 지표와 분리해서 해석
- Sentry: 확인 대상 아님

### 1차 해석

- 병목 의심 지점: 이번 run에서는 mixed가 아직 한계 구간에 들어서지 않았다.
- 근거 지표: read `p95 209.34ms`, write `p95 55.61ms`, 실패율 `0%`, 전체 요청 `59.87 req/s`
- 반례 / 불확실성: mixed 실제 한계 구간은 아직 미확정이며, `40/40` 이상에서 추가 확인이 필요할 수 있다.

### 다음 액션

- P0: prod-direct mixed는 현재 `30/30`까지 안정 구간으로 정리
- P1: 필요 시 `40/40` 이상 확장 여부 판단
- P2: prod-direct 확장 결과를 종합 문서에 반영

### 실행 신뢰도

- 신뢰도:
  - `high`
- 이유: mixed seed, snapshot restore, main run, 메타 기록이 모두 정상 동작했고 실패율도 `0%`였다.
## Round 39. v2 quick reset event-only 전환 및 M2 s2 재검증

### 목적

- `users DELETE` 때문에 과도하게 느려진 quick reset을 실서비스 가치가 낮은 FK 보조 인덱스 없이 완화한다.
- 변경 후 `W2 quick prepare`가 다시 정상 종료되는지 확인한다.
- 같은 변경이 `M2 s2` main 결과를 얼마나 바꾸는지 재검증한다.

### 변경

- `v2` quick reset에서 overlay user 삭제를 제거했다.
- quick reset은 `k6-v2` overlay event만 삭제한다.
- quick reset 검증도 event-only 전략에 맞게 바꿨다.
  - events는 baseline 동일
  - users는 baseline 이상이면 통과

### 결과

- `W2 quick prepare`
  - 정상 통과
- `M2 s2`
  - runId:
    - `m2-20260322-185539-s2-after-event-only-quick-reset`
  - 결과:
    - `threshold_fail`
  - read:
    - `p95 10.46s`
    - `p99 24.36s`
  - write:
    - `p95 8.47s`
    - `p99 10.07s`
  - achieved throughput:
    - `166.35 req/s`
  - dropped:
    - `8572`

### 해석

- 이번 변경은 `prepare/reset 병목 해소`에는 성공했다.
- 그러나 `M2 s2` 본체 병목은 여전히 남아 있다.
- 즉 다음 우선순위는 quick reset이 아니라 mixed main path 자체다.

## Round 41. Spring Cache + Caffeine 적용 후 M2 s2 재검증

### 목적

- route template / event type mapping 반복 조회를 조직별 메모리 캐시로 줄여 mixed read 쪽 shared DB work를 완화한다.
- cache 적용 직후 cold 상태와 steady-state rerun을 분리해서 기록한다.

### 변경

- Spring Cache + Caffeine 추가
- 조직별 `active route templates`, `active event type mappings` 캐시 적용
- route template / event type mapping 변경 시 해당 org 캐시 evict

### 결과

- cold cache 첫 run
  - runId:
    - `m2-20260322-203204-s2-after-caffeine-cache`
  - 결과:
    - `threshold_fail`
  - read:
    - `p95 17.10s`
    - `p99 43.17s`
  - write:
    - `p95 14.29s`
    - `p99 16.39s`
  - achieved throughput:
    - `121.64 req/s`
  - dropped:
    - `21999`
- steady-state rerun
  - runId:
    - `m2-20260322-204135-s2-rerun-after-caffeine`
  - 결과:
    - `threshold_fail`
  - read:
    - `p95 7.51s`
    - `p99 8.90s`
  - write:
    - `p95 7.19s`
    - `p99 8.34s`
  - achieved throughput:
    - `164.40 req/s`
  - dropped:
    - `8376`

### 해석

- 앱 재기동 직후 첫 run은 cold cache penalty가 커서 오히려 더 나빴다.
- 그러나 rerun은 event-only quick reset 기준 run보다 read/write `p95`가 내려가고 dropped도 소폭 줄었다.
- 즉 resolver 메타 조회 캐시는 steady-state에서는 일부 효과가 있었지만, `M2 s2`를 threshold 안으로 밀어 넣을 정도로 충분하진 않았다.
- 다음 우선순위는 resolver 메타 조회보다 집계/분석 쿼리 본체와 mixed shared DB work 쪽이다.
