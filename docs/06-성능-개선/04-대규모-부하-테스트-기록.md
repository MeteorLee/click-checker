# 04. 대규모 부하 테스트 기록

## 문서 목적

`03-대규모-부하-테스트-런북.md` 기준으로 수행한 부하 테스트 결과를 회차별로 누적 기록한다.  
이번 문서의 목적은 실행 결과를 단순 나열하는 것이 아니라, baseline과 개선 후 재테스트를 같은 형식으로 비교할 수 있게 증거를 축적하는 데 있다.

## 기록 원칙

- 회차마다 같은 항목을 같은 순서로 기록한다.
- 결과는 반드시 `수치 + 그래프 + 짧은 해석` 형태로 남긴다.
- 실행 시점의 커밋 해시 또는 이미지 태그를 남긴다.
- 테스트 데이터 식별 규칙(path prefix, eventType, runId)을 남긴다.
- baseline인지, 개선 후 재검증인지 먼저 명시한다.
- Grafana, Loki, Sentry에서 확인한 특이사항은 별도 메모로 남긴다.

## 현재 기준 baseline

- 기준 baseline run:
- 시나리오:
- 커밋 / 이미지:
- 환경:
- 기준 이유:

원칙:

- baseline이 바뀌면 이 섹션부터 먼저 갱신한다.
- 이후 run은 가능하면 `compareTo` 기준으로 이 baseline과 연결한다.

## 공통 기록 항목

- 실행 일시
- 대상 환경
- 대상 커밋 해시 / 이미지 태그
- 회차 성격
  - baseline
  - 개선 후 재검증
  - mixed 검증
- 시나리오
  - `W1`
  - `R1`
  - `R2`
  - `R3`
  - `M1`
- 부하 조건
  - warm-up
  - main run
  - cooldown
  - RPS 또는 단계 상승 조건
- 테스트 데이터 식별 규칙
- `k6` 결과
  - `http_req_failed`
  - `p95 / p99`
  - `RPS`
- 산출물 경로
- Grafana 확인 항목
  - latency
  - error rate
  - DB active connections
  - Hikari pool usage
- 로그 / Sentry 특이사항
- 1차 해석
- 다음 액션
- 실행 신뢰도

## Round 1~N 요약

- 초기 baseline과 주요 개선 run이 3개 이상 쌓이면 이 섹션을 먼저 갱신한다.
- 오래된 Round의 상세는 유지하되, 상단에는 요약만 배치한다.

## Round 1 - Baseline

### 실행 정보

- 실행 일시:
- 대상 환경:
- 대상 커밋 해시 / 이미지 태그:
- 회차 성격:
- 시나리오:

### 이전 Round 대비 변경점

- 

### 부하 조건

- warm-up:
- main run:
- cooldown:
- RPS / 단계 상승:

### 테스트 데이터 식별 규칙

- organization:
- path prefix:
- eventType:
- runId:

### k6 결과

- `http_req_failed`:
- `p95`:
- `p99`:
- 평균 RPS:
- 기타 요약:
- 산출물 디렉터리:
- `summary.json`:
- `console.log`:
- `meta.json`:

### Grafana 확인

- latency:
- error rate:
- DB active connections:
- Hikari pool usage:
- 캡처 시간 범위:
- 추가 캡처 메모:

### 로그 / Sentry 특이사항

- app log:
- nginx log:
- Sentry:

### 1차 해석

- 병목 의심 지점:
- 근거 지표:
- 반례 / 불확실성:

### 다음 액션

- P0:
- P1:
- P2:

### 실행 신뢰도

- 신뢰도:
  - `high | medium | low`
- 이유:

---

## Round 2

### 실행 정보

- 실행 일시:
- 대상 환경:
- 대상 커밋 해시 / 이미지 태그:
- 회차 성격:
- 시나리오:

### 이전 Round 대비 변경점

- 

### 부하 조건

- warm-up:
- main run:
- cooldown:
- RPS / 단계 상승:

### 테스트 데이터 식별 규칙

- organization:
- path prefix:
- eventType:
- runId:

### k6 결과

- `http_req_failed`:
- `p95`:
- `p99`:
- 평균 RPS:
- 기타 요약:
- 산출물 디렉터리:
- `summary.json`:
- `console.log`:
- `meta.json`:

### Grafana 확인

- latency:
- error rate:
- DB active connections:
- Hikari pool usage:
- 캡처 시간 범위:
- 추가 캡처 메모:

### 로그 / Sentry 특이사항

- app log:
- nginx log:
- Sentry:

### 1차 해석

- 병목 의심 지점:
- 근거 지표:
- 반례 / 불확실성:

### 다음 액션

- P0:
- P1:
- P2:

### 실행 신뢰도

- 신뢰도:
  - `high | medium | low`
- 이유:

---

## 누적 비교표

| Round | 시나리오 | 기준 | 변경 | 환경 | 커밋/이미지 | `http_req_failed` | `p95` | `p99` | 핵심 해석 | 다음 액션 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Round 1 |  | baseline |  |  |  |  |  |  |  |  |
| Round 2 |  |  |  |  |  |  |  |  |  |  |

## 문서 사용 원칙

- 실제 실험을 수행할 때는 기존 템플릿을 복사해 `Round 3`, `Round 4`처럼 누적한다.
- 같은 시나리오의 baseline과 개선 후 결과가 생기면 누적 비교표를 먼저 갱신한다.
- 실행 중 혼선이나 비정상 문제가 생기면 `성능-개선-트러블슈팅` 예정 문서에 분리 기록한다.
- 한 사이클(`테스트 -> 개선 -> 재테스트`)이 끝나면 `성능-개선-종합` 예정 문서에서 최종 정리한다.
