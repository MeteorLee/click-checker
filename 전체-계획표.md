# [PATCH v1] Click-Checker 향후 실행 계획표
(롤업 메인 트랙 반영 + 품질 기본선 분산 적용 + 관측 기반 보강 버전)

---

## 0단계 – 운영 관측 기반 최소 세트 확립 (Docker 기반)

🎯 목표
- 로컬에서도 “배포와 유사한 형태”로 재현 가능한 실행 환경 확보
- health/로그/메트릭/대시보드/부하 입력까지 한 번에 연결
- 이후 성능 개선 비교를 위한 기준선(베이스라인) 확보

---

### 0.1 Docker 기반 실행 골격 확립

- Dockerfile 구성
- docker-compose로 app + DB 실행
- (선택) 관측 스택까지 compose에 포함: Prometheus + Grafana
- health 체크 기반 기동 성공 판정

완료 기준:
- `docker compose up` 한 번으로 app+db 기동
- 앱 기동 성공을 health로 판정 가능

---

### 0.2 Actuator + Health 성공 판정 기준 확립

- Spring Actuator 도입
- `/actuator/health`를 로컬 검증/배포 성공 판정 기준으로 사용
- health에 DB 연결 상태 포함
- 운영/개발 환경에서 health detail 노출 정책 분리
- Actuator 노출은 기본 차단 + 허용 리스트(allowlist) 원칙 적용
  - 외부 공개 최소: `/actuator/health`
  - 내부 전용: `/actuator/prometheus` 등

완료 기준:
- DB 연결이 끊기면 health가 DOWN으로 바뀌는 것을 확인

#### 0.2.1 환경 프로파일/설정 분리 기준 (local / ci / prod)

- 프로파일별 “무조건 달라져야 하는 것” 목록을 고정한다.
  - DB URL/계정, Actuator 노출 범위, Sentry on/off, 로그 레벨, CORS, rate limit on/off 등
- 기본값은 “안전”이 되도록 한다. (prod에서 실수로 노출/전송되지 않게)

완료 기준:
- local/ci/prod 설정이 섞여도 치명적 사고(민감정보 노출/actuator 과다노출)가 발생하지 않음

---

### ### 0.3 로그 정책 정의 + 최소 구현 (Sentry 연계 정책 포함)

🎯 목적
- 요청 단위 추적 가능성 확보(requestId)
- 에러 발생 시 “로그 + Sentry” 조합으로 **원인 파악 속도**를 올린다.
- 민감정보 유출(로그/관측 도구)을 원천 차단한다.

---

#### 0.3.1 로그 기본 원칙

- 구조화 로그(JSON 권장)
- 요청 식별자(requestId)로 요청↔로그 상관관계 확보
- 정상 요청 로그는 INFO 최소화(또는 샘플링)
- 에러 로그는 stacktrace 포함 + requestId 필수
- 로그 레벨 기본:
  - 운영: INFO 최소 + WARN/ERROR 중심
  - 개발: DEBUG 선택적으로(기간 제한)

---

#### 0.3.2 requestId 정책(상관관계)

- 생성/전달:
  - 외부에서 `X-Request-Id`가 오면 사용, 없으면 서버에서 생성
- 전파:
  - 응답 헤더에도 `X-Request-Id`로 내려줌
- 로그 포함:
  - 모든 로그에 `requestId` 포함(MDC 등 활용)

완료 기준:
- 요청 1건에 대해 requestId가 로그에 찍히고,
  에러 상황에서 stacktrace + requestId로 추적 가능

---

#### 0.3.3 민감정보 마스킹/차단 정책 (로그 + Sentry 공통)

✅ 절대 기록/전송 금지(기본 차단 리스트)
- 인증/비밀 정보
  - `Authorization` 헤더(전체)
  - `Cookie` (세션/토큰 포함 가능)
  - `X-API-Key`, `apiKey` (원문)
  - Access/Refresh Token, JWT 원문
  - 비밀번호/패스워드, 시크릿, 키/서명값
- 개인정보(PII)
  - 이메일, 전화번호, 주민번호 등 식별 가능 정보
  - 사용자 입력 텍스트(원문) 중 PII 가능성이 큰 필드(정책 확정 필요)

✅ 허용(조건부) — “운영 맥락”으로 유용하지만 위험도 낮춘 형태
- `organizationId` (테넌트 식별자) → 태그/필드로 허용 가능
- `externalUserId` → 원문 저장 대신 **해시/부분 마스킹** 원칙 권장
- 요청 path / method / status / latency → 허용
- query string / request body → 기본 비활성(필요 시 allowlist 방식)

원칙:
- “기록/전송은 allowlist로, 기본은 차단”
- 마스킹은 최소한 다음 형태로:
  - 토큰류: `****` 전체 치환
  - 긴 식별자: 앞/뒤 일부만 남기고 마스킹(예: `ab***yz`)
  - 외부 ID: 가능하면 해시(SHA-256 등)로 변환해 상관관계만 유지

---

#### 0.3.4 Sentry 연계 정책 (로그와 역할 분리)

역할 분리
- 로그: “흐름/진단(요청 단위)” 중심
- Sentry: “예외/장애 원인(스택트레이스)” 중심

Sentry 전송 규칙(0단계 최소)
- Unhandled Exception은 Sentry로 자동 전송
- 태그 부여:
  - `service=click-checker`
  - `env=<local|dev|prod>`
  - `release=<git sha or version>`
  - `endpoint=<request path>`
  - `tenant_org_id=<organizationId>` (가능한 경우)
- 전송 금지 필드 적용(필수)
  - Authorization/apiKey/cookie/token/password 등 차단
  - request body/query는 기본 미전송(필요 시 allowlist로 제한)

알림 정책(선택)
- prod 환경만 알림 활성화(노이즈 방지)

완료 기준:
- 500 에러 1회 발생 시:
  - 로그에서 requestId로 요청 추적 가능
  - Sentry에서 동일 request의 스택트레이스/엔드포인트/env/release 확인 가능
- 민감정보가 로그/Sentry 어디에도 남지 않음을 샘플 점검

---

#### 0.3.5 로컬 보관/회전 최소 기준(0단계)

- 로컬은 단순 정책(예: 일 단위 파일 분리 + 최대 N일 보관)
- 운영은 후속 단계에서 중앙화/검색(예: ELK/CloudWatch 등)로 확장 가능
---

### 0.4 메트릭 수집 가능 상태 확보 (Micrometer → Prometheus)

- Micrometer Prometheus registry 추가
- `/actuator/prometheus` 노출 확인
- http.server.requests 메트릭 활성화
- p95/p99 측정을 위한 percentile/histogram 설정 활성화

완료 기준:
- `/actuator/prometheus`에서 요청/응답 관련 메트릭 확인
- p95/p99가 계산 가능한 형태로 노출됨

---

### 0.5 Prometheus + Grafana 로컬 대시보드 구성 (Docker)

- Prometheus를 docker-compose로 구성하고 앱 scrape 설정
- Grafana 연결
- “기본 대시보드(증거 수집용 최소 세트)” 구성
  - RPS
  - 평균 응답시간
  - p95 / p99
  - 4xx / 5xx 비율
  - HikariCP active connections(가능하면)

완료 기준:
- API 호출 시 그래프가 즉시 변동

---

### 0.6 초경량 Smoke Load 검증 (k6)

- k6 도입
- 고정 RPS 10~30
- duration 2~5분
- warm-up 30초(또는 초반 제외)

관찰 지표:
- p95 / p99 변화
- 4xx / 5xx 발생 여부
- HikariCP active connections 변화

완료 기준:
- 부하 시 p95/p99가 상승/변동하는 것을 확인
- 부하 종료 후 지표가 정상 범위로 복귀

#### 0.6.X k6 부하 시나리오 2종 고정(증거력 강화)

- Read-heavy 시나리오(집계 집중)
  - 목표: p95/p99 + DB 부하(커넥션/CPU) 변화 관찰
- Write-heavy 시나리오(수집/벌크 집중)
  - 목표: 커넥션/락/CPU/스레드풀 병목 관찰

완료 기준:
- “부하 테스트가 무엇을 의미하는지”가 시나리오 단위로 문서에 고정되고 반복 가능함

---

### 0.7 핵심 메트릭 기준 정의 (베이스라인)

- 요청 수(RPS)
- 응답시간: 평균 + p95/p99 병행
- 에러율: 4xx / 5xx 분리 관찰
- 성능 개선은 동일 RPS 조건 전/후 비교 원칙 확립
- “대시보드는 문제 해결 도구가 아니라 증거 수집 도구” 원칙 명시

---
### 0.8 Sentry 에러 트래킹 도입 (선택)

#### (추가) 0.8 Sentry 에러 트래킹 도입과의 연결 포인트

- 0.3에서 정의한 “민감정보 차단/allowlist” 정책이 Sentry 적용의 전제 조건
- 0.8 완료 시, 반드시 “Sentry 이벤트 payload에 민감정보가 없는지” 1회 검증 로그를 남긴다
  - (예) 테스트로 Authorization/apiKey 포함 요청을 일부러 보내고, Sentry에 원문이 남지 않는지 확인

🎯 목적
- “5xx가 늘었다”에서 끝나지 않고 **왜 터졌는지(스택트레이스/맥락)를 즉시 추적** 가능하게 한다.
- 운영 단계(특히 5단계 무중단/자동배포)에서 장애 탐지→원인 파악→수정 루프를 짧게 만든다.

도입 범위(0단계 최소)
- Spring Boot 예외 자동 수집(Unhandled Exception → Sentry 전송)
- 환경/버전 태깅
  - `environment=local/dev/prod`
  - `release=<git sha or version>`
- 이슈 그룹핑 확인(동일 예외 반복 시 1개 이슈로 묶임)

운영 맥락(태그/컨텍스트)
- tags(권장)
  - `service=click-checker`
  - `env=<local|dev|prod>`
  - `endpoint=<request path>`
  - `tenant_org_id=<organizationId>` (가능한 경우)
- 주의(필수)
  - API Key, Authorization 헤더, 토큰류, 개인정보는 **절대 전송/저장 금지**
  - 필요 시 Sentry 데이터 스크러빙/필터링 정책 우선 적용

완료 기준(검증 시나리오)
- 의도적으로 500 에러 1회 발생 → Sentry에 이벤트 수집 확인
  - stacktrace / endpoint / env / release 태그가 보임
- 동일 에러 10회 반복 → 하나의 이슈로 그룹핑 + 카운트 누적 확인
- release 변경 후 동일 에러 1회 → “어느 버전부터 발생” 추적 가능 확인

---

### 0단계 완료 정의 (Sentry 반영)

- docker-compose 기반으로 실행/재현 가능
- health로 성공 판정 가능
- 로그에 requestId 포함 + 민감정보 마스킹 원칙 확립
- 메트릭 수집/시각화 가능(Prometheus/Grafana)
- k6로 최소 부하를 넣고 p95/p99/에러율/커넥션 지표가 움직이는 것을 확인
- (선택) Sentry로 500 에러 수집/그룹핑/태깅(env/release)까지 검증 완료
- “측정 가능한 시스템” 상태 확보

---

## 1단계 – 배포 최소 루프 완성
(로컬 → CI → 수동 배포 → 자동(A))

#### 1.X 운영 진입점(외부 접근) 정의

- 외부에서 접근 가능한 진입점을 한 문장으로 고정한다.
  - 예: 수집 API는 API Key 필수, 집계 API는 관리자/JWT 또는 API Key 범위 제한
- 임시 접근 방식(orgId 쿼리 파라미터 등)을 언제 종료하는지 종료 조건을 명시한다.

완료 기준:
- “누가/무엇으로/어디에 접근하는가”가 문서에 1줄로 고정되어 있고, 임시 정책의 종료 조건이 적혀 있음


### 1.0 컨테이너 기반 실행 준비(Docker)

- Dockerfile 구성
- docker-compose로 app + DB 실행
- health 체크 기반 기동 성공 판정

- Prometheus + Grafana 로컬 구성
  - /actuator/prometheus 노출
  - RPS / p95 / p99 / 4xx / 5xx 기본 대시보드 구성
  - 향후 성능 개선 비교를 위한 측정 기반 확보
  - (PATCH) Micrometer 설정으로 p95/p99가 실제로 찍히도록(percentile/histogram) 활성화

### 1.1 로컬 배포 검증 구조 확립

- 배포 환경과 유사하게 실행
- DB 연결 / 이벤트 저장 / 집계 API 정상 동작 확인
- 배포 성공 기준 정의 (health + 이벤트 1건 저장)

### 1.2 CI 안정화 + 기본 품질선 확보

- 핵심 시나리오 테스트 (이벤트 수집 → DB 저장 → count 조회)
- CI 실패 시 배포 불가 원칙
- 공통 에러 응답 포맷 정의
- 글로벌 예외 처리 최소 골격 확립
- (PATCH) “Postgres에서만 터지는 케이스” 대비: Postgres 기반 통합 테스트 최소 1개(수단은 후속 결정)
  - 예: Testcontainers / docker-compose job / nightly 등 (선택)

### 1.3 수동 배포 1회 완주

- 서버 배포
- migrate 실행
- health 체크
- 이벤트 저장 확인

### 1.4 자동 배포(A: GitHub Actions 직접 배포, push형) 1회 완주

- main merge 시 자동 배포 트리거
- GitHub Actions에서 EC2로 원격 배포 수행(SSH 또는 SSM)
- 배포 흐름: build → test → migrate → deploy → health
- 실패 시 배포 중단 + 이전 버전으로 롤백 기준 정의
- 목적: 자동 배포 루프 1회 검증 및 운영 감각 확보
- (PATCH) migrate 실패 시 “앱 기동 금지 + 배포 중단” 원칙을 명시(부분 배포 방지)

#### 1.4.1 migrate 및 앱 기동 순서 규칙(강제)

- migrate 실패 시:
  - 배포 중단
  - 앱 기동 금지
  - 트래픽 전환(스위칭) 금지
- 배포 스크립트에서 순서를 강제한다:
  - build → test → migrate → deploy(start) → health/readiness

완료 기준:
- “migrate 성공”이 확인되지 않으면 앱이 기동/전환되지 않도록 자동화되어 있음

### 1.5 초경량 Smoke Load 검증

- k6를 활용한 고정 RPS 10~30 수준의 경량 부하 테스트 1회 수행
- duration 2~5분 유지
- Grafana에서 p95/p99/4xx/5xx 변화 확인
- DB 커넥션풀 지표 관찰
- 목적:
  - 메트릭이 실제로 움직이는 것 체감
  - 이후 성능 개선 비교의 기준선 확보

- 운영/데모 환경 실험 시 데이터 격리 원칙
  - test organization 고정 사용
  - 요청 path에 /__test/ prefix 사용
  - 필요 시 cleanup 루틴 확보

- (PATCH) Smoke 결과 비교 조건 고정(리포트 신뢰도 체크리스트)
  - 동일 RPS
  - 동일 duration + warm-up
  - 동일 데이터 조건(test org / path prefix)
  - 동일 배포 버전(커밋 해시 기록)
  - 동일 대시보드 시간창(스크린샷 범위 고정)


#### 1.5.1 테스트 데이터 정리(cleanup) 전략

- test organization / path prefix만으로는 장기적으로 DB 오염이 누적될 수 있으므로 정리 전략을 최소 1개 채택한다.
  - (A) TTL/리텐션 정책으로 테스트 데이터 자동 삭제
  - (B) nightly cleanup 배치(조건: orgId + /__test/ prefix)
- 정리 전략은 “운영/데모/부하” 실험 데이터에만 적용한다.

완료 기준:
- 데모/부하/스모크 데이터가 운영 데이터를 지속적으로 오염시키지 않음


---

## 2단계 – API Key 기반 인증 구조 완성

- Organization에 apiKey 도입
- apiKey 기반 Organization resolve 공통 로직 구현
- 모든 API에 organization scope 강제
- 401/403 정책 확립
- 키 생성 정책 정의
- 로그에 apiKey 원문 노출 금지
- (PATCH) apiKey 저장 방식 결정 포인트 명시
  - 원문 저장 vs 해시 저장(재확인 UX/유출 리스크 트레이드오프)
  - rotate(재발급) UX 및 정책(폐기/중복/유예) 정의

### 2.1 보안/테넌트 격리 가드레일

- 테넌트 격리 원칙 문서화
- organization scope 누락 방지 테스트 최소 1개 추가
- 민감정보 마스킹 정책 문서화

---

## 3단계 – Flyway 기반 스키마 통제 체계 확립

- 현재 스키마를 V1로 고정
- ddl-auto 제거
- 모든 변경은 migration 파일로 관리
- 배포 루틴에 migrate 포함
- (PATCH) migrate 실행 주체/권한 최소화 고려(전용 계정/권한 분리 가능성 명시)
- (PATCH) migration은 재실행 안전(idempotent) 원칙을 최대한 유지(운영 배포 안정성)

### 3.1 데이터 정합성 전략

- 컬럼 추가 → 백필 → NOT NULL 순서 준수
- 위험 DDL 2단계 배포 전략
- 마이그레이션 실패 대응 문서화
- 데이터 유실 방지 테스트 포함

---

## 4단계 – DB 분리 및 운영 환경 고정

- PostgreSQL → RDS 이전
- 시크릿/환경변수 분리
- 외부 접근 제한 정책
- RDS 기반 health 체크
- 백업/복구 리허설 1회 수행 및 문서화

- HikariCP 커넥션 풀 기본 정책 정의 및 관찰 포인트 정리
  - active connections
  - max pool size
  - connection timeout
  - p95 상승 시 커넥션 포화 여부 분석 기준 수립

---
## 5단계 – 웹 진입점 구성 및 운영 경로 정리

### 이번 단계의 목표 구조

- **Single EC2 1대**
- Nginx (HTTPS + Reverse Proxy)
- 단일 App 기동
- RDS: 공통 DB

> 이번 5단계는 “서비스 입구를 운영답게 만드는 단계”에 집중한다.  
> 운영형 자동 배포(B), Blue/Green, readiness 전환, 알림 고도화는 5.5단계로 분리한다.

---

### 구조 흐름

사용자  
→ Single EC2 (Nginx)  
→ Single EC2 내부 App  
→ RDS

---

## 5.1 Reverse Proxy + HTTPS

- Nginx 도입 (Single EC2)
- 내부 애플리케이션 포트 비노출
- Let’s Encrypt 적용
- HTTP → HTTPS 리다이렉트 설정
- Nginx 설정 파일 Git 관리

### 배포 시 안전 절차
1. `nginx -t` (문법 검사)
2. 통과 시 `nginx -s reload`
3. 실패 시 기존 설정 유지

---

## 5.2 엔드포인트 보호 정책

- `/actuator/health` 제한적 공개
- 내부 관리용 엔드포인트 차단
- 외부 공개 경로와 내부 관리 경로를 분리

### 중요 원칙
- Nginx는 진입점 제어와 경로 제한 역할을 담당한다.
- 운영 엔드포인트 노출 범위는 최소화한다.

---

## 5.3 운영 런북 작성

- Nginx 반영 절차 문서화
- HTTPS / 인증서 갱신 기본 절차 문서화
- 진입점 장애 대응 절차 문서화
- 롤백 전략 단계별 정리

---

## 5.5단계 – 운영형 자동 배포 고도화
(운영형 자동화(B) + 단일 EC2 기반 Blue/Green 무중단(유사 무중단) 배포)

### 목표 구조

- **Single EC2 1대**
- Nginx (HTTPS + Reverse Proxy)
- App Blue / Green 동시 기동 (서로 다른 포트 또는 컨테이너 2개)
- RDS: 공통 DB

> 단일 인스턴스 기반이므로 “무중단 배포 절차”는 충족하나,  
> 인스턴스 장애에 대한 고가용성(HA)은 범위 밖이다.  
> 즉, 배포 중 트래픽 무중단 목표는 달성할 수 있지만 EC2 자체 장애 시 전체 서비스 중단 가능성이 있다.

---

### 구조 흐름

사용자  
→ Single EC2 (Nginx)  
→ Single EC2 내부 App Blue / Green (8081 / 8082)  
→ RDS

---

## 5.5.1 운영형 자동 배포 (B: S3 / CodeDeploy 기반 Pull 배포)

- prod merge 시 배포 파이프라인 실행

### GitHub Actions 역할
- build
- test
- artifact 생성
- S3 업로드

### CodeDeploy 역할
- S3 artifact 수신
- lifecycle hook 기반 배포 수행
- 배포 성공/실패 상태 관리

### 배포 성공 판정
- `/actuator/health`
- readiness 통과

> readiness 판정은 `/actuator/health/readiness`를 기준으로 하며,  
> 해당 엔드포인트가 노출되도록 actuator health group/readiness 설정이 사전에 활성화되어 있어야 한다.

### 롤백 전략
- 이전 리비전으로 rollback
- health 재검증

### 배포 이력
- 리비전 단위 추적 가능 상태 확보

### (PATCH) 마이그레이션 실패 원칙
- migrate 단계 실패 시
- 트래픽 전환 금지
- 배포 중단

### 배포 그룹
- **single 배포 그룹**
- Single EC2 대상
- Nginx 설정 파일 포함
- Blue/Green 전환 스크립트 포함

---

## 5.5.2 무중단 배포 (단일 EC2 내부 Blue/Green)

단일 EC2에서 Blue/Green 구성으로 동시 운영 상태 확보

### 구성 예시
- Blue: 8081
- Green: 8082
- Nginx upstream이 8081 ↔ 8082 전환

---

### 배포 흐름 정의

1. 비활성 색상(Green)에 신버전 배포 및 기동
2. `/actuator/health/readiness` 200 확인
3. Nginx upstream 전환
4. `nginx -t` → `nginx -s reload`
5. 이전 색상(Blue) Graceful Shutdown
6. 전환 실패 시 즉시 기존 upstream(Blue)로 복구

---

### 전환 주체

- 전환은 Single EC2의 Nginx에서 수행
- App 프로세스는 트래픽을 직접 제어하지 않음

---

### 전환 로그 기록

- 전환 시각 기록
- 전환 전/후 health 결과 기록
- 전환 중 에러율 기록

> 배포 시점에 App이 2개 동시 실행되므로  
> CPU / 메모리 여유 확보 필요

---

## 5.5.3 Graceful Shutdown + Readiness 실전 검증

- `server.shutdown=graceful`
- `spring.lifecycle.timeout-per-shutdown-phase` 설정
- SIGTERM 수신 시 처리 중 요청 완료 후 종료 보장
- 요청 지속 발생 상태에서 배포 수행
- 전환 동안 5xx / timeout 미발생 확인
- p95 / p99 및 5xx 지표 기록

완료 기준:
- `read` 배포 실전 검증 통과
- `write` 배포 실전 검증 1차 실패 재현 및 원인 정황 확인
- old color drain 대기 추가 후 `write` 재검증 통과
- 런북 / 기록 / 트러블슈팅 / 종합 문서 정리 완료

후속 개선:
- old color drain / 종료 절차 고도화는 별도 개선 작업으로 분리한다.

---

## 5.5.4 Observability 실전 검증

- Prometheus / Grafana 운영 대시보드 확정
- Slack / Discord / Sentry 알림 연동
- 핵심 메트릭 기준 정의
- p95 / p99
- 4xx / 5xx 비율
- RPS
- 커넥션 풀 포화 여부

---

### 5.5.4.1 Observability 최소 알림 규칙 (3종)

- prod에서만 알림 활성화
- 최소 알림 3종 고정
- 5xx 비율 급증
- p95 급등
- DB 커넥션 풀 포화 또는 대기 증가

완료 기준:
- 배포 / 전환 / 장애 중 이상 징후 자동 감지
- 알림 노이즈 과도하지 않음

---

# 6️⃣ 6단계 – 집계 서비스 고도화

## 🎯 목표

- 집계 기준과 용어를 먼저 고정해 해석 흔들림을 줄인다
- Route / EventType / Time 축의 기본 분석 API를 제품 수준으로 정리한다
- 운영/성능 최적화 이전에 “쓸 수 있는 분석 기능”을 먼저 완성한다

---

## 6.0 집계 기준 / 용어 고정

- 시간 경계 고정
  - `from <= occurredAt < to`
  - bucket도 동일하게 `start <= t < end`
- timezone 기준 고정
  - API가 timezone을 명시적으로 받는지, 기본 timezone을 무엇으로 둘지 결정
- 빈 bucket 처리 고정
  - 시계열 응답은 요청한 전체 버킷을 채우고, 이벤트가 없으면 `count=0`
- unique user 정의 고정
  - 1차 기준을 무엇으로 볼지(eventUser / externalUserId 등) 먼저 결정
- route fallback 정책 고정
  - routeKey 미매칭은 rawPath를 그대로 쓰지 않고 `UNMATCHED_ROUTE` 같은 명시적 값으로 집계

> 목적: 이후 6단계 / 6.5단계의 모든 API가 같은 해석 규칙을 공유하게 한다

---

### 6.1 Route Template 기반 Path 정규화
- Organization별 Route Template CRUD
- rawPath → routeKey(normalizedPath) 변환
- 집계 기준을 path → routeKey로 전환
- query string 제거 및 숫자/UUID fallback 정규화
- rawPath 상위 목록 / 미매칭 샘플 조회
- 매칭 실패율(route miss rate) 모니터링

> 목적: Top N / 트렌드 정확도 확보  
> 의미: 데이터 품질 문제를 제품 기능으로 해결

---

### 6.2 EventType 축 정리

- raw eventType 목록 조회
- raw → canonical eventType 매핑 관리
- canonical eventType 기준 집계 축 정리
- mapping coverage 진단 지표 제공

> 목적: 퍼널/리텐션 전 단계에서 eventType 해석을 안정화한다

---

### 6.3 기본 KPI / 개요 집계

- overview API 제공
  - total events
  - unique users
  - eventType별 count
  - routeKey Top N
- 기간(from~to) 기반 count 확장
- previous period 비교 및 delta / deltaRate 계산 기준 고정

---

### 6.4 Route / EventType 분석 + 시간대별 트렌드

- routeKey 기준 Top N
- canonical eventType 기준 Top N
- routeKey × canonical eventType 교차 집계
- date_trunc 기반 시간대별 집계(hour/day)
- routeKey 기준 트렌드
- eventType 기준 트렌드

---

## 6.5단계 – 확장 분석 / 제품화

### 🎯 목표

- 사용자 단위 분석을 도입해 기본 집계를 한 단계 더 제품화한다
- Funnel / Retention / Cohort 같은 실무형 분석 API를 확장한다
- 응답 구조와 버저닝을 정리해 분석 API 계약을 안정화한다

---

### 6.5.1 사용자 분석

- 신규 / 재방문 사용자 분석
- user당 평균 이벤트 수
- routeKey별 unique users
- eventType별 unique users

---

### 6.5.2 Funnel

- canonical eventType 기준 funnel 정의
- step1-anchor 기준 분모 고정
- conversion window 정책 고정
- step별 최초 이벤트 인정 규칙 정의
- funnel report API 제공

---

### 6.5.3 Retention / Cohort

- first seen 또는 start event 기준 cohort 정의
- Day 1 / 7 / 30 retention 제공
- cohort report / retention report API 제공

---

### 6.5.4 집계 응답 구조 고도화

- 집계 결과 DTO 표준화
- 공통 메타 정보 포함
  - from / to
  - timezone
  - bucket 정보
  - appliedFilters
  - comparison 정보
- 향후 API 버전 확장을 고려한 응답 구조 설계

---

### 6.5.5 API 버저닝

- `/api/v1` prefix 적용
- breaking change 정책 문서화
- 버전 단위 호환성 정책 정의
- v2 설계 시 확장 지점 명시

> 목적: 집계 API 확장 대비 안정성 확보

---

## 7단계 – Account 및 Security 구조 도입

- 관리자 로그인 구현
- Spring Security + JWT
- 토큰 정책 정의
- 로그인/만료/재발급 정책 정리

---

## 8단계 – OrganizationMember + RBAC + 사용량/플랜 제품화

### 8.0 목표
- 7단계에서 만든 `Account + JWT` 인증 위에, 계정과 조직의 실제 관계를 올린다.
- `누가 로그인했는가` 다음으로 `어느 조직에서 무엇을 할 수 있는가`를 설명 가능한 구조로 만든다.
- 이후 관리자 콘솔, 플랜 제한, usage 집계가 기대는 권한/멤버십 기준을 먼저 고정한다.

---

### 8.1 OrganizationMember 구조
- `Account <-> Organization`을 직접 1:N으로 묶지 않고 `OrganizationMember`로 연결
- 한 계정의 다중 조직 소속 허용 여부 명시
- membership 최소 상태 정의
  - 예: `ACTIVE`, `REMOVED` 또는 `INVITED` 포함 여부 결정
- 현재 조직(current organization) 선택 방식 정의
  - 토큰 claim 포함 여부
  - request 기반 선택 여부
  - `/me` 응답에서 membership 목록 제공 여부
- 마지막 `OWNER` 보호 규칙 정의
  - 마지막 owner는 삭제/강등 금지 등
- 1차 멤버 관리 API 범위 정의
  - 목록
  - 추가/연결
  - 역할 변경
  - 제거/비활성화
- 이메일 초대/가입 승인 플로우는 이 단계에서 포함할지 제외할지 명시

---

### 8.2 RBAC 1차
- 최소 role 모델 정의
  - `OWNER`
  - `ADMIN`
  - `VIEWER`
- role별 책임 범위 문서화
  - 누가 API Key를 rotate할 수 있는가
  - 누가 route template / event type mapping을 수정할 수 있는가
  - 누가 analytics read만 가능한가
- `/api/v1/admin/**`에 membership + role 기반 인가 적용
- account 인증 성공 후, organization context가 없는 요청을 어떻게 처리할지 정의
- `401`과 `403` 기준을 membership/RBAC 기준으로 다시 정리
- 최소 인가 적용 대상 우선순위 정의
  - `/api/v1/admin/me`
  - 조직/멤버 관리 API
  - API Key 관리 API
  - route / event-type 설정 API
  - analytics admin read API

---

### 8.3 Organization API Key 1차

- organization 생성 이후 실제 ingest에 사용할 API Key를 어떻게 줄지 흐름 고정
- organization당 활성 API Key 1개 모델 유지 여부 정리
- plain key는 생성/rotate 시점에만 1회 노출하고, 이후에는 다시 보여주지 않는 정책 고정
- `OWNER`만 API Key 발급 / rotate 가능하도록 role 경계 정리
- 이후 admin 경로에서 보여줄 메타데이터 범위 정의
  - `kid`
  - `prefix`
  - `status`
  - `createdAt`
  - `rotatedAt`
  - `lastUsedAt`
- 기존 공개 organization 생성 경로와 새 admin organization 생성 경로의 관계 정리

---

### 8.4 관리자 콘솔(MVP)

- 로그인 기반 기본 대시보드 제공
- 현재 계정 / 현재 조직 상태 표시
- 조직 전환 UI 또는 organization context 선택 UI 제공
- 멤버 목록 / 역할 변경 UI 제공
- API Key 발급/폐기(rotate) UI 제공
- 조직별 usage/플랜 상태 표시
- 집계 요청 UI 실행 및 결과 확인

---

### 8단계 완료 정의(권장)
- `Account`가 하나 이상의 `Organization`에 membership으로 연결된다
- 최소 role(`OWNER / ADMIN / VIEWER`)이 코드와 문서에서 정의된다
- `/api/v1/admin/**` 일부 또는 전부가 membership + role 기준으로 인가된다
- organization과 API Key 관리 경계가 문서와 코드에서 설명 가능하다
- 이후 확장 주제를 후순위로 분리하더라도 방향을 설명 가능하다

---

## 9단계 – 대규모 운영 대응(대량 이벤트 유입 + 부하 테스트 + 롤업/리텐션)

### 9.0 목표
- 대량 이벤트 유입 시에도 서비스가 버티는 구조 확보(수집 보호/병목 제거)
- 부하 테스트로 병목을 “증거(메트릭/리포트)”로 남길 수 있는 상태
- 관측 데이터 저장/보존 전략까지 포함해 운영 확장 기반을 확보
- 데이터 보존/비용 통제를 위한 리텐션 + 롤업 체계 확립
- (선택) Kafka 없이 1차로 버티는 설계 → 필요 조건에서만 Kafka 트랙으로 확장

---

### 9.1 운영 관측 저장/보존 고도화
- Loki 저장소를 단일 EC2 filesystem에서 object storage(S3) 기반으로 확장할지 검토
- 로그 retention / compactor / 장기 보관 정책 정리
- 관측 데이터 보존 기간과 디스크/비용 기준 문서화
- Alertmanager 라우팅/알림 채널 운영 기준 보강
- Sentry release 태깅, 배포 버전 추적 기준 정리
- Sentry 이벤트에 requestId / appColor 같은 운영 추적 컨텍스트를 어디까지 연결할지 정리
- Sentry 잡음 필터링 기준(스캐너 404, 반복 노이즈)과 운영 해석 기준 보강
- 장애 알림은 계속 Prometheus + Alertmanager 중심으로 두고, Sentry는 예외 원인 추적 도구로 유지

완료 기준:
- 대규모 운영 대응 전에 로그 저장/보존 전략이 단일 디스크 의존에서 한 단계 분리됨
- 관측 데이터의 보존 기간과 운영 비용 기준을 설명 가능
- 배포 버전 / requestId 기준으로 로그와 예외를 더 쉽게 연결할 수 있다

---

### 9.2 부하 테스트(성능 “증거” 수집 단계)
- k6 / nGrinder
- TPS/RPS 측정
- DB CPU/커넥션/스레드풀 병목 확인
- p95/p99 비교, 4xx/5xx 관찰
- HikariCP 지표 관찰
- 메트릭 포함 리포트 작성(스크린샷 포함)
- RPS 고정 방식으로 전/후 비교
- 동일 조건(duration, warm-up) 유지
- Before / After 수치 비교 결과 문서화
- Grafana 대시보드 기반 스크린샷 포함

✅ (PATCH) 부하 테스트 비교 조건 고정(리포트 신뢰도 체크리스트)
- 동일 RPS
- 동일 duration + warm-up
- 동일 데이터 조건(test org / path prefix)
- 동일 배포 버전(커밋 해시 기록)
- 동일 대시보드 시간창(스크린샷 범위 고정)

---

#### 9.2.1 EXPLAIN 기반 인덱스 설계

- EXPLAIN ANALYZE 실행
- organization_id + occurred_at 복합 인덱스 점검
- routeKey 포함 인덱스 전략 검토
- 커버링 인덱스 가능성 분석
- 성능 개선 전/후 비교(k6 연계 가능)

---

### 9.3 Kafka 없이 1차 병목 대응 전략(수집 보호)
- 이벤트 테이블은 append-only 성격 유지
- 수집 테이블 인덱스 최소화 원칙 적용 (쓰기 성능 보호)
- Batch insert 전략 검토 (Hibernate/JDBC batch)
- 집계는 실시간 계산 대신 롤업/배치 기반 분리
- 과부하 시 org 단위 레이트 리밋 적용
- “롤업/배치 기반 분리” 의미 명확화
  - 실시간 집계는 비용 큼 → 확정 구간은 rollup 조회로 전환
  - 최신 구간은 raw 기반(또는 raw+rollup 혼합)으로 정의(정확도/성능 트레이드오프 문서화)

#### 9.3.1 Redis 기반 rate limit
- org 단위 요청 제한
- 분산 환경 일관성 확보
- Redis 역할: 쓰기/수집 보호(백프레셔)
- (선택) 과부하 시 응답 정책(429) + Retry-After 헤더 정책 문서화

#### 9.3.2 Bulk ingest API
- 배열 수신 구조
- insert 최적화 전략
- (선택) 부분 실패/유효성 검증 비용 정책 문서화(전체 실패 vs 부분 성공)

---

### 9.4 읽기 경로 최적화

#### 9.4.1 Redis 기반 캐시 도입 (읽기 부하 절감)
- 집계 API 결과 TTL 캐싱
- 캐시 적용 전/후 응답 시간 및 DB 부하 비교
- 캐시 키 설계 원칙
  - orgId + routeKey + from/to + bucket + eventType + externalUserId + apiVersion
- usage 정책과 캐시 정책 연결
  - TTL 내 동일 요청 재조회는 usage 미차감
- near real-time 정의
  - 최신 포함 집계: 짧은 TTL
  - 과거 확정 구간 집계: 긴 TTL
- Redis 장애 시 fallback: DB 조회 (캐시 실패가 기능 장애로 이어지지 않게)

---

### 9.5 데이터 보존 및 롤업 전략(비용/성능/정합성)
- 리텐션 정책 정의
- 오래된 이벤트 삭제/아카이빙 전략

#### 9.5.1 Spring Batch 도입(롤업/리텐션의 실행체)
- 정기 리텐션 삭제 배치 + 실패 재시도 전략
- 일/시간 단위 롤업 테이블 생성 및 정기 집계 배치 구현
- 롤업 정합성 검증 및 재집계 전략 문서화

(보강) 배치 멱등성/재실행 안전 원칙
- rollup_key 기준 upsert
- 마지막 처리 시점(cursor) 저장으로 중복/누락 방지
- late arrival 허용 범위/재집계 정책 정의

---

### 9단계 완료 정의(권장)
- 대량 유입 시나리오(수집/집계)를 부하로 재현 가능
- 병목이 “그래프 + 수치”로 설명 가능(p95/p99, 5xx, 커넥션풀)
- 롤업/리텐션이 배치로 실행되고 재실행 안전 원칙이 문서화됨
- “최신 구간 vs 확정 구간” 집계 전략이 명시되어 있음

---

## 10단계 – 품질 및 안정성 고도화

### 10.0 목표
- 기능을 더 붙이는 단계에서 끝내지 않고, 실제로 깨지지 않는지 확인하는 품질 기본선을 세운다.
- 인증/인가/admin/analytics 흐름이 각각 테스트와 운영 검증 기준을 갖도록 만든다.
- 이후 기능 추가가 곧 품질 저하로 이어지지 않도록 최소 회귀 방지선을 만든다.

---

### 10.1 Postgres 기반 통합 테스트 확장
- `Postgres 기반 통합 테스트 1개 이상` 원칙을 계속 유지
- auth + membership + RBAC 흐름도 Postgres 기준 통합 테스트 추가
- H2와 Postgres 결과 차이가 날 수 있는 쿼리/DDL 포인트 점검
- Testcontainers 도입 범위를 확대할지 결정

---

### 10.2 핵심 시나리오 E2E 회귀선
- 조직 생성 -> 계정 로그인 -> current org 선택 -> 권한 체크 -> analytics 호출 시나리오 정리
- login / refresh / logout / me 전체 흐름 회귀 검증
- API Key + JWT 혼합 경계 회귀 검증
- 잘못된 role / membership 차단 시나리오 회귀 검증

---

### 10.3 배포 후 최소 smoke 체계 고도화
- 현재 health/smoke 외에 auth/admin smoke를 추가할지 검토
- 배포 후 확인해야 하는 최소 admin 시나리오 정의
  - login 성공
  - `/api/v1/admin/me`
  - role 필요한 경로 차단
- k6 또는 curl 기반 최소 smoke 스크립트 정리

---

### 10.4 관측 지표 / 알림 기준 정교화
- auth 실패율, refresh 실패율, admin 4xx/5xx 비율 관찰 기준 추가
- role 차단(`403`)과 실제 오류(`5xx`)를 운영에서 구분할 수 있게 정리
- login/refresh/logout 경로에 대해 어떤 메트릭과 로그를 남길지 고정

---

### 10.5 문서 / 포트폴리오 마감 기준 정리
- README에 어떤 수준까지 반영할지 기준 정리
- 공개 문서와 내부 작업 문서를 어디서 분리할지 확정
- 면접 설명 기준으로 30초 / 2분 / 5분 설명 포인트 정리

---

### 10단계 완료 정의(권장)
- 핵심 auth/admin/analytics 시나리오가 테스트 또는 smoke로 재현 가능
- Postgres 기준 회귀선이 존재한다
- auth/admin 경로 운영 지표와 장애 판단 기준이 정리돼 있다
- 포트폴리오 공개 문서와 내부 문서의 경계가 명확하다

---

## 포트폴리오 완성을 위해 후순위로 미루는 항목

- 아래 항목은 "하지 않는다"가 아니라, 1차 포트폴리오 완성 범위에서 우선순위를 낮춘다.
- 현재는 `핵심 분석 기능 + 운영/배포/관측 + 최소 auth/RBAC + 품질/문서 마감`을 우선한다.
- 구현보다 문서/설명/시연 완성도가 더 중요해지는 시점이 오면, 아래 항목은 후속 확장으로 넘긴다.

### 제품 기능 확장
- 아래 항목은 원래 8단계의 확장 주제였지만, 1차 포트폴리오 완성 범위에서는 후순위로 둔다.

#### 기존 8.3 조직별 usage 집계
- usage를 무엇으로 셀지 먼저 고정
  - ingest 이벤트 수
  - analytics read 요청 수
  - active member 수
  - 기타 과금/제한 후보
- usage 집계 단위 정의
  - 일간
  - 월간
  - billing month 기준
- 차감 시점 정의
  - 요청 수신 시
  - 성공 처리 시
  - 비동기 완료 시
- cache hit 시 usage 차감 여부 정의
- 조직 단위 usage 조회 API 또는 내부 관리 모델 정의
- 관리자 콘솔/플랜 정책이 사용할 usage source of truth 결정

#### 기존 8.4 플랜 및 요청 제한 정책
- 최소 플랜 모델 정의
  - Free
  - Pro
  - Team
  - 또는 그에 준하는 단순 2~3단계 모델
- 플랜별 제한 항목 정의
  - 월 이벤트 수
  - read API 호출 수
  - 멤버 수
  - 기능 제한 여부
- 초과 시 정책 정의
  - hard block
  - soft warning
  - grace period
- `429` 응답 정책 정의
  - 응답 바디
  - `Retry-After`
  - 어떤 제한에 대해 429를 줄지
- 플랜 변경 시점과 usage 초기화 기준 정리

#### 기존 8.5 Audit Log
- 키 재발급 기록
- 권한 변경 기록
- 플랜 변경 기록
- 조직 멤버 추가/제거 기록
- 최소 감사 로그 저장 범위와 조회 필요성 정리

#### 기존 8.6 SDK / 예제
- 계정 로그인 curl 예제
- refresh / logout curl 예제
- 조직별 admin API curl 예제
- JS 예제
- Postman 컬렉션

- 이메일 초대 / 가입 승인 / 비밀번호 재설정
- organization lifecycle 전체 구현
  - 즉시 삭제
  - 비활성화 상세 정책

### 보안 / 권한 구조 고도화
- Spring Security principal / `SecurityContext` 중심 구조로의 정식 전환
- `@PreAuthorize`, `PermissionEvaluator` 같은 정식 인가 구조
- API Key 다중 발급 / 세밀한 권한별 키 관리

### 화면 / 데모 확장
- 관리자 콘솔의 대형 확장
- usage / 플랜 / 운영 설정까지 포함한 복합 관리 화면
- 화면 수를 늘리는 방향의 확장보다, 분석 결과 시연 화면 품질을 우선한다

### 선택적 인프라 확장
- Redis 기반 캐시 / rate limit의 본격 도입
- Kafka 도입
- ECS / Fargate 전환

---

# 완성 이후 업그레이드 트랙

## GitHub Actions 액션 런타임 업그레이드

- GitHub Actions JavaScript 액션의 Node.js 24 전환 일정 확인
- `actions/checkout`, `actions/setup-java`, `aws-actions/configure-aws-credentials`, `aws-actions/amazon-ecr-login`의 호환 버전 점검
- 필요 시 워크플로우 액션 버전 업그레이드 후 prod 배포 1회 검증

## ECS 도입

- Fargate 전환
- ALB 기반 트래픽 분산
- CloudWatch 연동

## Kafka 도입 (선택)

- 이벤트 비동기 파이프라인 확장
- 도입 조건:
  - DB를 쓰기 버퍼로 사용하는 구조가 한계에 도달
  - 유실 없는 비동기 처리 요구
  - 이벤트 소비자가 복수(저장/집계/알림/분석 등)로 증가
  - fan-out 구조 필요

## 집계 고도화 (확정 구간 + 최신 구간 분리 전략)

- 집계를 확정 구간 / 최신 구간으로 분리 처리
- 확정 구간은 롤업 기반 처리
- 최신 구간은 실시간 델타 계산
- late arrival 허용 범위 정의
