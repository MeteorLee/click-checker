spring:
  datasource:
    url: jdbc:h2:mem:click_checker_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  api-key:
    env: test
    pepper: ci-test-pepper
